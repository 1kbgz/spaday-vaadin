# Generated by spaday.cem from custom-elements.json — do not edit by hand.
from __future__ import annotations

from typing import Any

from spaday.catalog import ComponentSchema, PropertySchema
from spaday.component import Child, Component

__all__ = [
    "VaadinButton",
    "VaadinCheckbox",
    "VaadinComboBox",
    "VaadinDatePicker",
    "VaadinDialog",
    "VaadinGrid",
    "VaadinGridColumn",
    "VaadinGridColumnGroup",
    "VaadinGridFilter",
    "VaadinGridFilterColumn",
    "VaadinGridSelectionColumn",
    "VaadinGridSortColumn",
    "VaadinGridSorter",
    "VaadinGridTreeColumn",
    "VaadinGridTreeToggle",
    "VaadinNotification",
    "VaadinSelect",
    "VaadinTab",
    "VaadinTabs",
    "VaadinTextField",
]


class VaadinButton(Component):
    """`<vaadin-button>` is an accessible and customizable button that allows users to perform actions. ```html <vaadin-button>Press me</vaadin-button> ``` ### Styling The following shadow DOM parts are available for styling: Part name | Description ----------|------------- `label` | The label (text) inside the button. `prefix` | A slot for content before the label (e.g. an icon). `suffix` | A slot for content after the label (e.g. an icon). The following state attributes are available for styling: Attribute | Description ---------------|------------- `active` | Set when the button is pressed down, either with mouse, touch or the keyboard `disabled` | Set when the button is disabled `focus-ring` | Set when the button is focused using the keyboard `focused` | Set when the button is focused `has-tooltip` | Set when the button has a slotted tooltip The following custom CSS properties are available for styling: Custom CSS property | :----------------------------------| | `--vaadin-button-background` | | `--vaadin-button-border-color` | | `--vaadin-button-border-radius` | | `--vaadin-button-border-width` | | `--vaadin-button-font-family` | | `--vaadin-button-font-size` | | `--vaadin-button-font-weight` | | `--vaadin-button-gap` | | `--vaadin-button-height` | | `--vaadin-button-label-wrap` | | `--vaadin-button-line-height` | | `--vaadin-button-margin` | | `--vaadin-button-padding` | | `--vaadin-button-text-color` | See [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation."""

    tag = "vaadin-button"
    schema = ComponentSchema(
        tag="vaadin-button",
        class_name="VaadinButton",
        summary="`<vaadin-button>` is an accessible and customizable button that allows users to perform actions.\n\n```html\n<vaadin-button>Press me</vaadin-button>\n```\n\n### Styling\n\nThe following shadow DOM parts are available for styling:\n\nPart name | Description\n----------|-------------\n`label`   | The label (text) inside the button.\n`prefix`  | A slot for content before the label (e.g. an icon).\n`suffix`  | A slot for content after the label (e.g. an icon).\n\nThe following state attributes are available for styling:\n\nAttribute      | Description\n---------------|-------------\n`active`       | Set when the button is pressed down, either with mouse, touch or the keyboard\n`disabled`     | Set when the button is disabled\n`focus-ring`   | Set when the button is focused using the keyboard\n`focused`      | Set when the button is focused\n`has-tooltip`  | Set when the button has a slotted tooltip\n\nThe following custom CSS properties are available for styling:\n\nCustom CSS property                |\n:----------------------------------|\n| `--vaadin-button-background`     |\n| `--vaadin-button-border-color`   |\n| `--vaadin-button-border-radius`  |\n| `--vaadin-button-border-width`   |\n| `--vaadin-button-font-family`    |\n| `--vaadin-button-font-size`      |\n| `--vaadin-button-font-weight`    |\n| `--vaadin-button-gap`            |\n| `--vaadin-button-height`         |\n| `--vaadin-button-label-wrap`     |\n| `--vaadin-button-line-height`    |\n| `--vaadin-button-margin`         |\n| `--vaadin-button-padding`        |\n| `--vaadin-button-text-color`     |\n\nSee [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation.",
        props=(
            PropertySchema(
                name="disabled",
                kind="boolean",
                choices=(),
                default=None,
                description="When disabled, the button is rendered as \"dimmed\".\n\nBy default, disabled buttons are not focusable and don't react to hover.\nAs a result, they are hidden from assistive technologies, and it's not\npossible to show a tooltip to explain why they are disabled. This can\nbe addressed by enabling the feature flag `accessibleDisabledButtons`,\nwhich makes disabled buttons focusable and hoverable, while still\npreventing them from being activated:\n\n```js\n// Set before any button is attached to the DOM.\nwindow.Vaadin.featureFlags.accessibleDisabledButtons = true\n```",
            ),
            PropertySchema(
                name="theme",
                kind="string",
                choices=(),
                default=None,
                description="Space-separated theme variants, such as `primary` or `small`, which the element propagates to its parts.",
            ),
        ),
        events=(),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        disabled: bool | None = None,
        theme: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "disabled": disabled,
                "theme": theme,
            },
            **props,
        )


class VaadinCheckbox(Component):
    """`<vaadin-checkbox>` is an input field representing a binary choice. ```html <vaadin-checkbox label="I accept the terms and conditions"></vaadin-checkbox> ``` ### Styling The following shadow DOM parts are available for styling: Part name | Description ---------------------|------------- `checkbox` | The element representing a stylable custom checkbox `label` | The slotted label element wrapper `helper-text` | The slotted helper text element wrapper `error-message` | The slotted error message element wrapper `required-indicator` | The `required` state indicator element The following state attributes are available for styling: Attribute | Description ---------------------|------------- `active` | Set when the checkbox is activated with mouse, touch or the keyboard. `checked` | Set when the checkbox is checked. `disabled` | Set when the checkbox is disabled. `readonly` | Set when the checkbox is readonly. `focus-ring` | Set when the checkbox is focused using the keyboard. `focused` | Set when the checkbox is focused. `indeterminate` | Set when the checkbox is in the indeterminate state. `invalid` | Set when the checkbox is invalid. `has-label` | Set when the checkbox has a label. `has-helper` | Set when the checkbox has helper text. `has-error-message` | Set when the checkbox has an error message. `has-tooltip` | Set when the checkbox has a slotted tooltip. The following custom CSS properties are available for styling: Custom CSS property | :--------------------------------------------------| | `--vaadin-checkbox-background` | | `--vaadin-checkbox-border-color` | | `--vaadin-checkbox-border-radius` | | `--vaadin-checkbox-border-width` | | `--vaadin-checkbox-gap` | | `--vaadin-checkbox-label-color` | | `--vaadin-checkbox-label-font-size` | | `--vaadin-checkbox-label-font-weight` | | `--vaadin-checkbox-label-line-height` | | `--vaadin-checkbox-marker-color` | | `--vaadin-checkbox-marker-size` | | `--vaadin-checkbox-size` | | `--vaadin-input-field-error-color` | | `--vaadin-input-field-error-font-size` | | `--vaadin-input-field-error-font-weight` | | `--vaadin-input-field-error-line-height` | | `--vaadin-input-field-helper-color` | | `--vaadin-input-field-helper-font-size` | | `--vaadin-input-field-helper-font-weight` | | `--vaadin-input-field-helper-line-height` | | `--vaadin-input-field-required-indicator` | | `--vaadin-input-field-required-indicator-color` | See [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation."""

    tag = "vaadin-checkbox"
    schema = ComponentSchema(
        tag="vaadin-checkbox",
        class_name="VaadinCheckbox",
        summary='`<vaadin-checkbox>` is an input field representing a binary choice.\n\n```html\n<vaadin-checkbox label="I accept the terms and conditions"></vaadin-checkbox>\n```\n\n### Styling\n\nThe following shadow DOM parts are available for styling:\n\nPart name            | Description\n---------------------|-------------\n`checkbox`           | The element representing a stylable custom checkbox\n`label`              | The slotted label element wrapper\n`helper-text`        | The slotted helper text element wrapper\n`error-message`      | The slotted error message element wrapper\n`required-indicator` | The `required` state indicator element\n\nThe following state attributes are available for styling:\n\nAttribute            | Description\n---------------------|-------------\n`active`             | Set when the checkbox is activated with mouse, touch or the keyboard.\n`checked`            | Set when the checkbox is checked.\n`disabled`           | Set when the checkbox is disabled.\n`readonly`           | Set when the checkbox is readonly.\n`focus-ring`         | Set when the checkbox is focused using the keyboard.\n`focused`            | Set when the checkbox is focused.\n`indeterminate`      | Set when the checkbox is in the indeterminate state.\n`invalid`            | Set when the checkbox is invalid.\n`has-label`          | Set when the checkbox has a label.\n`has-helper`         | Set when the checkbox has helper text.\n`has-error-message`  | Set when the checkbox has an error message.\n`has-tooltip`        | Set when the checkbox has a slotted tooltip.\n\nThe following custom CSS properties are available for styling:\n\nCustom CSS property                                |\n:--------------------------------------------------|\n| `--vaadin-checkbox-background`                   |\n| `--vaadin-checkbox-border-color`                 |\n| `--vaadin-checkbox-border-radius`                |\n| `--vaadin-checkbox-border-width`                 |\n| `--vaadin-checkbox-gap`                          |\n| `--vaadin-checkbox-label-color`                  |\n| `--vaadin-checkbox-label-font-size`              |\n| `--vaadin-checkbox-label-font-weight`            |\n| `--vaadin-checkbox-label-line-height`            |\n| `--vaadin-checkbox-marker-color`                 |\n| `--vaadin-checkbox-marker-size`                  |\n| `--vaadin-checkbox-size`                         |\n| `--vaadin-input-field-error-color`               |\n| `--vaadin-input-field-error-font-size`           |\n| `--vaadin-input-field-error-font-weight`         |\n| `--vaadin-input-field-error-line-height`         |\n| `--vaadin-input-field-helper-color`              |\n| `--vaadin-input-field-helper-font-size`          |\n| `--vaadin-input-field-helper-font-weight`        |\n| `--vaadin-input-field-helper-line-height`        |\n| `--vaadin-input-field-required-indicator`        |\n| `--vaadin-input-field-required-indicator-color`  |\n\nSee [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation.',
        props=(
            PropertySchema(
                name="accessible-name",
                kind="string",
                choices=(),
                default=None,
                description="String used to label the component to screen reader users.",
            ),
            PropertySchema(
                name="accessible-name-ref",
                kind="string",
                choices=(),
                default=None,
                description="Id of the element used as label of the component to screen reader users.",
            ),
            PropertySchema(
                name="autofocus",
                kind="boolean",
                choices=(),
                default=None,
                description="Specify that this control should have input focus when the page loads.",
            ),
            PropertySchema(name="checked", kind="boolean", choices=(), default=None, description="True if the element is checked."),
            PropertySchema(
                name="disabled", kind="boolean", choices=(), default=None, description="If true, the user cannot interact with this element."
            ),
            PropertySchema(name="error-message", kind="string", choices=(), default=None, description="Error to show when the field is invalid."),
            PropertySchema(name="helper-text", kind="string", choices=(), default=None, description="String used for the helper text."),
            PropertySchema(
                name="indeterminate",
                kind="boolean",
                choices=(),
                default=None,
                description="True if the checkbox is in the indeterminate state which means\nit is not possible to say whether it is checked or unchecked.\nThe state is reset once the user switches the checkbox by hand.\n\nhttps://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/checkbox#Indeterminate_state_checkboxes",
            ),
            PropertySchema(name="invalid", kind="boolean", choices=(), default=None, description="Set to true when the field is invalid."),
            PropertySchema(
                name="label",
                kind="string",
                choices=(),
                default=None,
                description="The label text for the input node.\nWhen no light dom defined via [slot=label], this value will be used.",
            ),
            PropertySchema(
                name="manual-validation",
                kind="boolean",
                choices=(),
                default=None,
                description="Set to true to enable manual validation mode. This mode disables automatic\nconstraint validation, allowing you to control the validation process yourself.\nYou can still trigger constraint validation manually with the `validate()` method\nor use `checkValidity()` to assess the component's validity without affecting\nthe invalid state. In manual validation mode, you can also manipulate\nthe `invalid` property directly through your application logic without conflicts\nwith the component's internal validation.",
            ),
            PropertySchema(name="name", kind="string", choices=(), default=None, description="The name of the checkbox."),
            PropertySchema(
                name="readonly",
                kind="boolean",
                choices=(),
                default=None,
                description="When true, the user cannot modify the value of the checkbox.\nThe difference between `disabled` and `readonly` is that the\nread-only checkbox remains focusable, is announced by screen\nreaders and its value can be submitted as part of the form.",
            ),
            PropertySchema(name="required", kind="boolean", choices=(), default=None, description="Specifies that the user must fill in a value."),
            PropertySchema(name="value", kind="string", choices=(), default=None, description="The value of the field."),
            PropertySchema(
                name="theme",
                kind="string",
                choices=(),
                default=None,
                description="Space-separated theme variants, such as `primary` or `small`, which the element propagates to its parts.",
            ),
        ),
        events=("change", "checked-changed", "indeterminate-changed", "invalid-changed", "validated"),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        accessible_name: str | None = None,
        accessible_name_ref: str | None = None,
        autofocus: bool | None = None,
        checked: bool | None = None,
        disabled: bool | None = None,
        error_message: str | None = None,
        helper_text: str | None = None,
        indeterminate: bool | None = None,
        invalid: bool | None = None,
        label: str | None = None,
        manual_validation: bool | None = None,
        name: str | None = None,
        readonly: bool | None = None,
        required: bool | None = None,
        value: str | None = None,
        theme: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "accessible-name": accessible_name,
                "accessible-name-ref": accessible_name_ref,
                "autofocus": autofocus,
                "checked": checked,
                "disabled": disabled,
                "error-message": error_message,
                "helper-text": helper_text,
                "indeterminate": indeterminate,
                "invalid": invalid,
                "label": label,
                "manual-validation": manual_validation,
                "name": name,
                "readonly": readonly,
                "required": required,
                "value": value,
                "theme": theme,
            },
            **props,
        )


class VaadinComboBox(Component):
    """`<vaadin-combo-box>` is a web component for choosing a value from a filterable list of options presented in a dropdown overlay. The options can be provided as a list of strings or objects by setting [`items`](#/elements/vaadin-combo-box#property-items) property on the element. ```html <vaadin-combo-box id="combo-box"></vaadin-combo-box> ``` ```js document.querySelector('#combo-box').items = ['apple', 'orange', 'banana']; ``` When the selected `value` is changed, a `value-changed` event is triggered. ### Item rendering To customize the content of the `<vaadin-combo-box-item>` elements placed in the dropdown, use [`renderer`](#/elements/vaadin-combo-box#property-renderer) property which accepts a function. The renderer function is called with `root`, `comboBox`, and `model` as arguments. Generate DOM content by using `model` object properties if needed, and append it to the `root` element. The `comboBox` reference is provided to access the combo-box element state. Do not set combo-box properties in a `renderer` function. ```js const comboBox = document.querySelector('#combo-box'); comboBox.items = [{'label': 'Hydrogen', 'value': 'H'}]; comboBox.renderer = (root, comboBox, model) => { const item = model.item; root.innerHTML = `${model.index}: ${item.label} <b>${item.value}</b>`; }; ``` Renderer is called on the opening of the combo-box and each time the related model is updated. Before creating new content, it is recommended to check if there is already an existing DOM element in `root` from a previous renderer call for reusing it. Even though combo-box uses infinite scrolling, reducing DOM operations might improve performance. The following properties are available in the `model` argument: Property | Type | Description -----------|------------------|------------- `index` | Number | Index of the item in the `items` array `item` | String or Object | The item reference `selected` | Boolean | True when item is selected `focused` | Boolean | True when item is focused ### Lazy Loading with Function Data Provider In addition to assigning an array to the items property, you can alternatively use the [`dataProvider`](#/elements/vaadin-combo-box#property-dataProvider) function property. The `<vaadin-combo-box>` calls this function lazily, only when it needs more data to be displayed. __Note that when using function data providers, the total number of items needs to be set manually. The total number of items can be returned in the second argument of the data provider callback:__ ```js comboBox.dataProvider = async (params, callback) => { const API = 'https://demo.vaadin.com/demo-data/1.0/filtered-countries'; const { filter, page, pageSize } = params; const index = page * pageSize; const res = await fetch(`${API}?index=${index}&count=${pageSize}&filter=${filter}`); if (res.ok) { const { result, size } = await res.json(); callback(result, size); } }; ``` ### Styling The following shadow DOM parts are available for styling: Part name | Description ---------------------|---------------- `label` | The label element `input-field` | The element that wraps prefix, value and buttons `field-button` | Set on both clear and toggle buttons `clear-button` | The clear button `error-message` | The error message element `helper-text` | The helper text element wrapper `required-indicator` | The `required` state indicator element `toggle-button` | The toggle button `overlay` | The overlay container `content` | The overlay content `loader` | The loading indicator shown while loading items The following state attributes are available for styling: Attribute | Description ---------------------|--------------------------------- `disabled` | Set when the element is disabled `has-value` | Set when the element has a value `has-label` | Set when the element has a label `has-helper` | Set when the element has helper text or slot `has-error-message` | Set when the element has an error message `has-tooltip` | Set when the element has a slotted tooltip `invalid` | Set when the element is invalid `focused` | Set when the element is focused `focus-ring` | Set when the element is keyboard focused `readonly` | Set when the element is readonly `opened` | Set when the overlay is opened `loading` | Set when loading items from the data provider The following custom CSS properties are available for styling: Custom CSS property | :--------------------------------------------------| | `--vaadin-combo-box-overlay-max-height` | | `--vaadin-combo-box-overlay-width` | | `--vaadin-field-default-width` | | `--vaadin-input-field-background` | | `--vaadin-input-field-border-color` | | `--vaadin-input-field-border-radius` | | `--vaadin-input-field-border-width` | | `--vaadin-input-field-bottom-end-radius` | | `--vaadin-input-field-bottom-start-radius` | | `--vaadin-input-field-button-text-color` | | `--vaadin-input-field-container-gap` | | `--vaadin-input-field-disabled-background` | | `--vaadin-input-field-disabled-text-color` | | `--vaadin-input-field-error-color` | | `--vaadin-input-field-error-font-size` | | `--vaadin-input-field-error-font-weight` | | `--vaadin-input-field-error-line-height` | | `--vaadin-input-field-gap` | | `--vaadin-input-field-helper-color` | | `--vaadin-input-field-helper-font-size` | | `--vaadin-input-field-helper-font-weight` | | `--vaadin-input-field-helper-line-height` | | `--vaadin-input-field-label-color` | | `--vaadin-input-field-label-font-size` | | `--vaadin-input-field-label-font-weight` | | `--vaadin-input-field-label-line-height` | | `--vaadin-input-field-padding` | | `--vaadin-input-field-placeholder-color` | | `--vaadin-input-field-required-indicator` | | `--vaadin-input-field-required-indicator-color` | | `--vaadin-input-field-top-end-radius` | | `--vaadin-input-field-top-start-radius` | | `--vaadin-input-field-value-color` | | `--vaadin-input-field-value-font-size` | | `--vaadin-input-field-value-font-weight` | | `--vaadin-input-field-value-line-height` | | `--vaadin-item-overlay-padding` | ### Internal components In addition to `<vaadin-combo-box>` itself, the following internal components are themable: - `<vaadin-combo-box-item>` - has the same API as [`<vaadin-item>`](#/elements/vaadin-item). See [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation."""

    tag = "vaadin-combo-box"
    schema = ComponentSchema(
        tag="vaadin-combo-box",
        class_name="VaadinComboBox",
        summary="`<vaadin-combo-box>` is a web component for choosing a value from a filterable list of options\npresented in a dropdown overlay. The options can be provided as a list of strings or objects\nby setting [`items`](#/elements/vaadin-combo-box#property-items) property on the element.\n\n```html\n<vaadin-combo-box id=\"combo-box\"></vaadin-combo-box>\n```\n\n```js\ndocument.querySelector('#combo-box').items = ['apple', 'orange', 'banana'];\n```\n\nWhen the selected `value` is changed, a `value-changed` event is triggered.\n\n### Item rendering\n\nTo customize the content of the `<vaadin-combo-box-item>` elements placed in the dropdown, use\n[`renderer`](#/elements/vaadin-combo-box#property-renderer) property which accepts a function.\nThe renderer function is called with `root`, `comboBox`, and `model` as arguments.\n\nGenerate DOM content by using `model` object properties if needed, and append it to the `root`\nelement. The `comboBox` reference is provided to access the combo-box element state. Do not\nset combo-box properties in a `renderer` function.\n\n```js\nconst comboBox = document.querySelector('#combo-box');\ncomboBox.items = [{'label': 'Hydrogen', 'value': 'H'}];\ncomboBox.renderer = (root, comboBox, model) => {\n  const item = model.item;\n  root.innerHTML = `${model.index}: ${item.label} <b>${item.value}</b>`;\n};\n```\n\nRenderer is called on the opening of the combo-box and each time the related model is updated.\nBefore creating new content, it is recommended to check if there is already an existing DOM\nelement in `root` from a previous renderer call for reusing it. Even though combo-box uses\ninfinite scrolling, reducing DOM operations might improve performance.\n\nThe following properties are available in the `model` argument:\n\nProperty   | Type             | Description\n-----------|------------------|-------------\n`index`    | Number           | Index of the item in the `items` array\n`item`     | String or Object | The item reference\n`selected` | Boolean          | True when item is selected\n`focused`  | Boolean          | True when item is focused\n\n### Lazy Loading with Function Data Provider\n\nIn addition to assigning an array to the items property, you can alternatively use the\n[`dataProvider`](#/elements/vaadin-combo-box#property-dataProvider) function property.\nThe `<vaadin-combo-box>` calls this function lazily, only when it needs more data\nto be displayed.\n\n__Note that when using function data providers, the total number of items\nneeds to be set manually. The total number of items can be returned\nin the second argument of the data provider callback:__\n\n```js\ncomboBox.dataProvider = async (params, callback) => {\n  const API = 'https://demo.vaadin.com/demo-data/1.0/filtered-countries';\n  const { filter, page, pageSize } = params;\n  const index = page * pageSize;\n\n  const res = await fetch(`${API}?index=${index}&count=${pageSize}&filter=${filter}`);\n  if (res.ok) {\n    const { result, size } = await res.json();\n    callback(result, size);\n  }\n};\n```\n\n### Styling\n\nThe following shadow DOM parts are available for styling:\n\nPart name            | Description\n---------------------|----------------\n`label`              | The label element\n`input-field`        | The element that wraps prefix, value and buttons\n`field-button`       | Set on both clear and toggle buttons\n`clear-button`       | The clear button\n`error-message`      | The error message element\n`helper-text`        | The helper text element wrapper\n`required-indicator` | The `required` state indicator element\n`toggle-button`      | The toggle button\n`overlay`            | The overlay container\n`content`            | The overlay content\n`loader`             | The loading indicator shown while loading items\n\nThe following state attributes are available for styling:\n\nAttribute            | Description\n---------------------|---------------------------------\n`disabled`           | Set when the element is disabled\n`has-value`          | Set when the element has a value\n`has-label`          | Set when the element has a label\n`has-helper`         | Set when the element has helper text or slot\n`has-error-message`  | Set when the element has an error message\n`has-tooltip`        | Set when the element has a slotted tooltip\n`invalid`            | Set when the element is invalid\n`focused`            | Set when the element is focused\n`focus-ring`         | Set when the element is keyboard focused\n`readonly`           | Set when the element is readonly\n`opened`             | Set when the overlay is opened\n`loading`            | Set when loading items from the data provider\n\nThe following custom CSS properties are available for styling:\n\nCustom CSS property                                |\n:--------------------------------------------------|\n| `--vaadin-combo-box-overlay-max-height`          |\n| `--vaadin-combo-box-overlay-width`               |\n| `--vaadin-field-default-width`                   |\n| `--vaadin-input-field-background`                |\n| `--vaadin-input-field-border-color`              |\n| `--vaadin-input-field-border-radius`             |\n| `--vaadin-input-field-border-width`              |\n| `--vaadin-input-field-bottom-end-radius`         |\n| `--vaadin-input-field-bottom-start-radius`       |\n| `--vaadin-input-field-button-text-color`         |\n| `--vaadin-input-field-container-gap`             |\n| `--vaadin-input-field-disabled-background`       |\n| `--vaadin-input-field-disabled-text-color`       |\n| `--vaadin-input-field-error-color`               |\n| `--vaadin-input-field-error-font-size`           |\n| `--vaadin-input-field-error-font-weight`         |\n| `--vaadin-input-field-error-line-height`         |\n| `--vaadin-input-field-gap`                       |\n| `--vaadin-input-field-helper-color`              |\n| `--vaadin-input-field-helper-font-size`          |\n| `--vaadin-input-field-helper-font-weight`        |\n| `--vaadin-input-field-helper-line-height`        |\n| `--vaadin-input-field-label-color`               |\n| `--vaadin-input-field-label-font-size`           |\n| `--vaadin-input-field-label-font-weight`         |\n| `--vaadin-input-field-label-line-height`         |\n| `--vaadin-input-field-padding`                   |\n| `--vaadin-input-field-placeholder-color`         |\n| `--vaadin-input-field-required-indicator`        |\n| `--vaadin-input-field-required-indicator-color`  |\n| `--vaadin-input-field-top-end-radius`            |\n| `--vaadin-input-field-top-start-radius`          |\n| `--vaadin-input-field-value-color`               |\n| `--vaadin-input-field-value-font-size`           |\n| `--vaadin-input-field-value-font-weight`         |\n| `--vaadin-input-field-value-line-height`         |\n| `--vaadin-item-overlay-padding`                  |\n\n### Internal components\n\nIn addition to `<vaadin-combo-box>` itself, the following internal\ncomponents are themable:\n\n- `<vaadin-combo-box-item>` - has the same API as [`<vaadin-item>`](#/elements/vaadin-item).\n\nSee [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation.",
        props=(
            PropertySchema(
                name="accessible-name",
                kind="string",
                choices=(),
                default=None,
                description="String used to label the component to screen reader users.",
            ),
            PropertySchema(
                name="accessible-name-ref",
                kind="string",
                choices=(),
                default=None,
                description="Id of the element used as label of the component to screen reader users.",
            ),
            PropertySchema(
                name="allow-custom-value",
                kind="boolean",
                choices=(),
                default=None,
                description="If `true`, the user can input a value that is not present in the items list.\n`value` property will be set to the input value in this case.\nAlso, when `value` is set programmatically, the input value will be set\nto reflect that value.",
            ),
            PropertySchema(
                name="allowed-char-pattern",
                kind="string",
                choices=(),
                default=None,
                description="A pattern matched against individual characters the user inputs.\n\nWhen set, the field will prevent:\n- `keydown` events if the entered key doesn't match `/^allowedCharPattern$/`\n- `paste` events if the pasted text doesn't match `/^allowedCharPattern*$/`\n- `drop` events if the dropped text doesn't match `/^allowedCharPattern*$/`\n\nFor example, to allow entering only numbers and minus signs, use:\n`allowedCharPattern = \"[\\\\d-]\"`",
            ),
            PropertySchema(
                name="auto-open-disabled",
                kind="boolean",
                choices=(),
                default=None,
                description="Set true to prevent the overlay from opening automatically.",
            ),
            PropertySchema(
                name="autofocus",
                kind="boolean",
                choices=(),
                default=None,
                description="Specify that this control should have input focus when the page loads.",
            ),
            PropertySchema(
                name="autoselect",
                kind="boolean",
                choices=(),
                default=None,
                description="If true, the input text gets fully selected when the field is focused using click or touch / tap.",
            ),
            PropertySchema(
                name="clear-button-visible",
                kind="boolean",
                choices=(),
                default=None,
                description="Set to true to display the clear icon which clears the input.\nThis also enables clearing the input when pressing Esc key.",
            ),
            PropertySchema(
                name="data-provider",
                kind="json",
                choices=(),
                type_text="ComboBoxDataProvider | undefined",
                default=None,
                description="Function that provides items lazily. Receives arguments `params`, `callback`\n\n`params.page` Requested page index\n\n`params.pageSize` Current page size\n\n`params.filter` Currently applied filter\n\n`callback(items, size)` Callback function with arguments:\n  - `items` Current page of items\n  - `size` Total number of items.",
            ),
            PropertySchema(
                name="disabled", kind="boolean", choices=(), default=None, description="If true, the user cannot interact with this element."
            ),
            PropertySchema(name="error-message", kind="string", choices=(), default=None, description="Error to show when the field is invalid."),
            PropertySchema(
                name="filter", kind="string", choices=(), default=None, description="Filtering string the user has typed into the input field."
            ),
            PropertySchema(name="helper-text", kind="string", choices=(), default=None, description="String used for the helper text."),
            PropertySchema(name="invalid", kind="boolean", choices=(), default=None, description="Set to true when the field is invalid."),
            PropertySchema(
                name="item-id-path",
                kind="string",
                choices=(),
                default=None,
                description="Path for the id of the item. If `items` is an array of objects,\nthe `itemIdPath` is used to compare and identify the same item\nin `selectedItem` and `filteredItems` (items given by the\n`dataProvider` callback).",
            ),
            PropertySchema(
                name="item-label-path",
                kind="string",
                choices=(),
                default=None,
                description="Path for label of the item. If `items` is an array of objects, the\n`itemLabelPath` is used to fetch the displayed string label for each\nitem.\n\nThe item label is also used for matching items when processing user\ninput, i.e., for filtering and selecting items.",
            ),
            PropertySchema(
                name="item-value-path",
                kind="string",
                choices=(),
                default=None,
                description="Path for the value of the item. If `items` is an array of objects, the\n`itemValuePath:` is used to fetch the string value for the selected\nitem.\n\nThe item value is used in the `value` property of the combo box,\nto provide the form value.",
            ),
            PropertySchema(
                name="label",
                kind="string",
                choices=(),
                default=None,
                description="The label text for the input node.\nWhen no light dom defined via [slot=label], this value will be used.",
            ),
            PropertySchema(
                name="loading",
                kind="boolean",
                choices=(),
                default=None,
                description='When set to `true`, "loading" attribute is added to host and the overlay element.',
            ),
            PropertySchema(
                name="manual-validation",
                kind="boolean",
                choices=(),
                default=None,
                description="Set to true to enable manual validation mode. This mode disables automatic\nconstraint validation, allowing you to control the validation process yourself.\nYou can still trigger constraint validation manually with the `validate()` method\nor use `checkValidity()` to assess the component's validity without affecting\nthe invalid state. In manual validation mode, you can also manipulate\nthe `invalid` property directly through your application logic without conflicts\nwith the component's internal validation.",
            ),
            PropertySchema(name="name", kind="string", choices=(), default=None, description="The name of this field."),
            PropertySchema(name="opened", kind="boolean", choices=(), default=None, description="True if the dropdown is open, false otherwise."),
            PropertySchema(
                name="page-size", kind="number", choices=(), default=None, description="Number of items fetched at a time from the dataprovider."
            ),
            PropertySchema(
                name="pattern",
                kind="string",
                choices=(),
                default=None,
                description="A regular expression that the value is checked against.\nThe pattern must match the entire value, not just some subset.",
            ),
            PropertySchema(
                name="placeholder", kind="string", choices=(), default=None, description="A hint to the user of what can be entered in the field."
            ),
            PropertySchema(
                name="readonly", kind="boolean", choices=(), default=None, description="When present, it specifies that the field is read-only."
            ),
            PropertySchema(
                name="renderer",
                kind="json",
                choices=(),
                type_text="ComboBoxRenderer | undefined",
                default=None,
                description="Custom function for rendering the content of every item.\nReceives three arguments:\n\n- `root` The `<vaadin-combo-box-item>` internal container DOM element.\n- `comboBox` The reference to the `<vaadin-combo-box>` element.\n- `model` The object with the properties related with the rendered\n  item, contains:\n  - `model.index` The index of the rendered item.\n  - `model.item` The item.",
            ),
            PropertySchema(name="required", kind="boolean", choices=(), default=None, description="Specifies that the user must fill in a value."),
            PropertySchema(
                name="selected-item",
                kind="json",
                choices=(),
                type_text="ComboBoxItem | string | undefined",
                default=None,
                description="The selected item from the `items` array.",
            ),
            PropertySchema(name="size", kind="number", choices=(), default=None, description="Total number of items."),
            PropertySchema(
                name="title",
                kind="string",
                choices=(),
                default=None,
                description="The text usually displayed in a tooltip popup when the mouse is over the field.",
            ),
            PropertySchema(name="value", kind="string", choices=(), default=None, description="The value of the field."),
            PropertySchema(
                name="theme",
                kind="string",
                choices=(),
                default=None,
                description="Space-separated theme variants, such as `primary` or `small`, which the element propagates to its parts.",
            ),
        ),
        fields=(
            PropertySchema(
                name="filteredItems",
                kind="json",
                choices=(),
                type_text="Array<ComboBoxItem | string> | undefined",
                default=None,
                description="A subset of items, filtered based on the user input. Filtered items\ncan be assigned directly to omit the internal filtering functionality.\nThe items can be of either `String` or `Object` type.",
            ),
            PropertySchema(
                name="itemClassNameGenerator",
                kind="json",
                choices=(),
                type_text="object",
                default=None,
                description="A function used to generate CSS class names for dropdown\nitems based on the item. The return value should be the\ngenerated class name as a string, or multiple class names\nseparated by whitespace characters.",
            ),
            PropertySchema(
                name="itemLabelGenerator",
                kind="json",
                choices=(),
                type_text="object",
                default=None,
                description="A function that is used to generate the label for dropdown\nitems based on the item. Receives one argument:\n- `item` The item to generate the label for.",
            ),
            PropertySchema(
                name="items",
                kind="json",
                choices=(),
                type_text="Array<ComboBoxItem | string> | undefined",
                default=None,
                description="A full set of items to filter the visible options from.\nThe items can be of either `String` or `Object` type.",
            ),
        ),
        events=(
            "change",
            "custom-value-set",
            "filter-changed",
            "invalid-changed",
            "opened-changed",
            "selected-item-changed",
            "vaadin-combo-box-dropdown-closed",
            "vaadin-combo-box-dropdown-opened",
            "validated",
            "value-changed",
        ),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        accessible_name: str | None = None,
        accessible_name_ref: str | None = None,
        allow_custom_value: bool | None = None,
        allowed_char_pattern: str | None = None,
        auto_open_disabled: bool | None = None,
        autofocus: bool | None = None,
        autoselect: bool | None = None,
        clear_button_visible: bool | None = None,
        data_provider: Any = None,
        disabled: bool | None = None,
        error_message: str | None = None,
        filter: str | None = None,
        helper_text: str | None = None,
        invalid: bool | None = None,
        item_id_path: str | None = None,
        item_label_path: str | None = None,
        item_value_path: str | None = None,
        label: str | None = None,
        loading: bool | None = None,
        manual_validation: bool | None = None,
        name: str | None = None,
        opened: bool | None = None,
        page_size: float | None = None,
        pattern: str | None = None,
        placeholder: str | None = None,
        readonly: bool | None = None,
        renderer: Any = None,
        required: bool | None = None,
        selected_item: Any = None,
        size: float | None = None,
        title: str | None = None,
        value: str | None = None,
        theme: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "accessible-name": accessible_name,
                "accessible-name-ref": accessible_name_ref,
                "allow-custom-value": allow_custom_value,
                "allowed-char-pattern": allowed_char_pattern,
                "auto-open-disabled": auto_open_disabled,
                "autofocus": autofocus,
                "autoselect": autoselect,
                "clear-button-visible": clear_button_visible,
                "data-provider": data_provider,
                "disabled": disabled,
                "error-message": error_message,
                "filter": filter,
                "helper-text": helper_text,
                "invalid": invalid,
                "item-id-path": item_id_path,
                "item-label-path": item_label_path,
                "item-value-path": item_value_path,
                "label": label,
                "loading": loading,
                "manual-validation": manual_validation,
                "name": name,
                "opened": opened,
                "page-size": page_size,
                "pattern": pattern,
                "placeholder": placeholder,
                "readonly": readonly,
                "renderer": renderer,
                "required": required,
                "selected-item": selected_item,
                "size": size,
                "title": title,
                "value": value,
                "theme": theme,
            },
            **props,
        )


class VaadinDatePicker(Component):
    """`<vaadin-date-picker>` is an input field that allows to enter a date by typing or by selecting from a calendar overlay. ```html <vaadin-date-picker label="Birthday"></vaadin-date-picker> ``` ```js datePicker.value = '2016-03-02'; ``` When the selected `value` is changed, a `value-changed` event is triggered. ### Styling The following shadow DOM parts are available for styling: Part name | Description ---------------------|---------------- `label` | The label element `input-field` | The element that wraps prefix, value and buttons `field-button` | Set on both clear and toggle buttons `clear-button` | The clear button `error-message` | The error message element `helper-text` | The helper text element wrapper `required-indicator` | The `required` state indicator element `toggle-button` | The toggle button `backdrop` | Backdrop of the overlay `overlay` | The overlay container `content` | The overlay content The following state attributes are available for styling: Attribute | Description ---------------------|--------------------------------- `disabled` | Set when the element is disabled `has-value` | Set when the element has a value `has-label` | Set when the element has a label `has-helper` | Set when the element has helper text or slot `has-error-message` | Set when the element has an error message `has-tooltip` | Set when the element has a slotted tooltip `invalid` | Set when the element is invalid `focused` | Set when the element is focused `focus-ring` | Set when the element is keyboard focused `readonly` | Set when the element is readonly `opened` | Set when the overlay is opened `week-numbers` | Set when week numbers are shown in the calendar The following custom CSS properties are available for styling: Custom CSS property | :----------------------------------------------------------| | `--vaadin-date-picker-date-border-radius` | | `--vaadin-date-picker-date-disabled-color` | | `--vaadin-date-picker-date-height` | | `--vaadin-date-picker-date-selected-background` | | `--vaadin-date-picker-date-selected-color` | | `--vaadin-date-picker-date-today-color` | | `--vaadin-date-picker-date-width` | | `--vaadin-date-picker-month-header-color` | | `--vaadin-date-picker-month-header-font-size` | | `--vaadin-date-picker-month-header-font-weight` | | `--vaadin-date-picker-month-padding` | | `--vaadin-date-picker-overlay-max-height` | | `--vaadin-date-picker-overlay-width` | | `--vaadin-date-picker-toolbar-padding` | | `--vaadin-date-picker-week-divider-color` | | `--vaadin-date-picker-week-number-color` | | `--vaadin-date-picker-week-number-font-size` | | `--vaadin-date-picker-weekday-color` | | `--vaadin-date-picker-weekday-font-size` | | `--vaadin-date-picker-weekday-font-weight` | | `--vaadin-date-picker-year-scroller-background` | | `--vaadin-date-picker-year-scroller-border-color` | | `--vaadin-date-picker-year-scroller-current-year-color` | | `--vaadin-date-picker-year-scroller-width` | | `--vaadin-field-default-width` | | `--vaadin-input-field-background` | | `--vaadin-input-field-border-color` | | `--vaadin-input-field-border-radius` | | `--vaadin-input-field-border-width` | | `--vaadin-input-field-bottom-end-radius` | | `--vaadin-input-field-bottom-start-radius` | | `--vaadin-input-field-button-text-color` | | `--vaadin-input-field-container-gap` | | `--vaadin-input-field-disabled-background` | | `--vaadin-input-field-disabled-text-color` | | `--vaadin-input-field-error-color` | | `--vaadin-input-field-error-font-size` | | `--vaadin-input-field-error-font-weight` | | `--vaadin-input-field-error-line-height` | | `--vaadin-input-field-gap` | | `--vaadin-input-field-helper-color` | | `--vaadin-input-field-helper-font-size` | | `--vaadin-input-field-helper-font-weight` | | `--vaadin-input-field-helper-line-height` | | `--vaadin-input-field-label-color` | | `--vaadin-input-field-label-font-size` | | `--vaadin-input-field-label-font-weight` | | `--vaadin-input-field-label-line-height` | | `--vaadin-input-field-padding` | | `--vaadin-input-field-placeholder-color` | | `--vaadin-input-field-required-indicator` | | `--vaadin-input-field-required-indicator-color` | | `--vaadin-input-field-top-end-radius` | | `--vaadin-input-field-top-start-radius` | | `--vaadin-input-field-value-color` | | `--vaadin-input-field-value-font-size` | | `--vaadin-input-field-value-font-weight` | | `--vaadin-input-field-value-line-height` | ### Internal components In addition to `<vaadin-date-picker>` itself, the following internal components are themable: - `<vaadin-date-picker-overlay-content>` - `<vaadin-date-picker-month-scroller>` - `<vaadin-date-picker-year-scroller>` - `<vaadin-date-picker-year>` - `<vaadin-month-calendar>` In order to style the overlay content, use `<vaadin-date-picker-overlay-content>` shadow DOM parts: Part name | Description ----------------------|-------------------- `years-toggle-button` | Fullscreen mode years scroller toggle `toolbar` | Toolbar with slotted buttons The following state attributes are available on the `<vaadin-date-picker-overlay-content>` element: Attribute | Description ----------------|------------------------------------------------- `desktop` | Set when the overlay content is in desktop mode `fullscreen` | Set when the overlay content is in fullscreen mode `years-visible` | Set when the year scroller is visible in fullscreen mode In order to style the month calendar, use `<vaadin-month-calendar>` shadow DOM parts: Part name | Description ----------------------|-------------------- `month-header` | Month title `weekdays` | Weekday container `weekday` | Weekday element `week-numbers` | Week numbers container `week-number` | Week number element `date` | Date element `disabled` | Disabled date element `focused` | Focused date element `selected` | Selected date element `today` | Date element corresponding to the current day `past` | Date element corresponding to the date in the past `future` | Date element corresponding to the date in the future In order to style year scroller elements, use `<vaadin-date-picker-year>` shadow DOM parts: Part name | Description ----------------------|-------------------- `year-number` | Year number `year-separator` | Year separator See [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation. ### Change events Depending on the nature of the value change that the user attempts to commit e.g. by pressing Enter, the component can fire either a `change` event or an `unparsable-change` event: Value change | Event :------------------------|:------------------ empty => parsable | change empty => unparsable | unparsable-change parsable => empty | change parsable => parsable | change parsable => unparsable | change unparsable => empty | unparsable-change unparsable => parsable | change unparsable => unparsable | unparsable-change"""

    tag = "vaadin-date-picker"
    schema = ComponentSchema(
        tag="vaadin-date-picker",
        class_name="VaadinDatePicker",
        summary="`<vaadin-date-picker>` is an input field that allows to enter a date by typing or by selecting from a calendar overlay.\n\n```html\n<vaadin-date-picker label=\"Birthday\"></vaadin-date-picker>\n```\n\n```js\ndatePicker.value = '2016-03-02';\n```\n\nWhen the selected `value` is changed, a `value-changed` event is triggered.\n\n### Styling\n\nThe following shadow DOM parts are available for styling:\n\nPart name            | Description\n---------------------|----------------\n`label`              | The label element\n`input-field`        | The element that wraps prefix, value and buttons\n`field-button`       | Set on both clear and toggle buttons\n`clear-button`       | The clear button\n`error-message`      | The error message element\n`helper-text`        | The helper text element wrapper\n`required-indicator` | The `required` state indicator element\n`toggle-button`      | The toggle button\n`backdrop`           | Backdrop of the overlay\n`overlay`            | The overlay container\n`content`            | The overlay content\n\nThe following state attributes are available for styling:\n\nAttribute            | Description\n---------------------|---------------------------------\n`disabled`           | Set when the element is disabled\n`has-value`          | Set when the element has a value\n`has-label`          | Set when the element has a label\n`has-helper`         | Set when the element has helper text or slot\n`has-error-message`  | Set when the element has an error message\n`has-tooltip`        | Set when the element has a slotted tooltip\n`invalid`            | Set when the element is invalid\n`focused`            | Set when the element is focused\n`focus-ring`         | Set when the element is keyboard focused\n`readonly`           | Set when the element is readonly\n`opened`             | Set when the overlay is opened\n`week-numbers`       | Set when week numbers are shown in the calendar\n\nThe following custom CSS properties are available for styling:\n\nCustom CSS property                                        |\n:----------------------------------------------------------|\n| `--vaadin-date-picker-date-border-radius`                |\n| `--vaadin-date-picker-date-disabled-color`               |\n| `--vaadin-date-picker-date-height`                       |\n| `--vaadin-date-picker-date-selected-background`          |\n| `--vaadin-date-picker-date-selected-color`               |\n| `--vaadin-date-picker-date-today-color`                  |\n| `--vaadin-date-picker-date-width`                        |\n| `--vaadin-date-picker-month-header-color`                |\n| `--vaadin-date-picker-month-header-font-size`            |\n| `--vaadin-date-picker-month-header-font-weight`          |\n| `--vaadin-date-picker-month-padding`                     |\n| `--vaadin-date-picker-overlay-max-height`                |\n| `--vaadin-date-picker-overlay-width`                     |\n| `--vaadin-date-picker-toolbar-padding`                   |\n| `--vaadin-date-picker-week-divider-color`                |\n| `--vaadin-date-picker-week-number-color`                 |\n| `--vaadin-date-picker-week-number-font-size`             |\n| `--vaadin-date-picker-weekday-color`                     |\n| `--vaadin-date-picker-weekday-font-size`                 |\n| `--vaadin-date-picker-weekday-font-weight`               |\n| `--vaadin-date-picker-year-scroller-background`          |\n| `--vaadin-date-picker-year-scroller-border-color`        |\n| `--vaadin-date-picker-year-scroller-current-year-color`  |\n| `--vaadin-date-picker-year-scroller-width`               |\n| `--vaadin-field-default-width`                           |\n| `--vaadin-input-field-background`                        |\n| `--vaadin-input-field-border-color`                      |\n| `--vaadin-input-field-border-radius`                     |\n| `--vaadin-input-field-border-width`                      |\n| `--vaadin-input-field-bottom-end-radius`                 |\n| `--vaadin-input-field-bottom-start-radius`               |\n| `--vaadin-input-field-button-text-color`                 |\n| `--vaadin-input-field-container-gap`                     |\n| `--vaadin-input-field-disabled-background`               |\n| `--vaadin-input-field-disabled-text-color`               |\n| `--vaadin-input-field-error-color`                       |\n| `--vaadin-input-field-error-font-size`                   |\n| `--vaadin-input-field-error-font-weight`                 |\n| `--vaadin-input-field-error-line-height`                 |\n| `--vaadin-input-field-gap`                               |\n| `--vaadin-input-field-helper-color`                      |\n| `--vaadin-input-field-helper-font-size`                  |\n| `--vaadin-input-field-helper-font-weight`                |\n| `--vaadin-input-field-helper-line-height`                |\n| `--vaadin-input-field-label-color`                       |\n| `--vaadin-input-field-label-font-size`                   |\n| `--vaadin-input-field-label-font-weight`                 |\n| `--vaadin-input-field-label-line-height`                 |\n| `--vaadin-input-field-padding`                           |\n| `--vaadin-input-field-placeholder-color`                 |\n| `--vaadin-input-field-required-indicator`                |\n| `--vaadin-input-field-required-indicator-color`          |\n| `--vaadin-input-field-top-end-radius`                    |\n| `--vaadin-input-field-top-start-radius`                  |\n| `--vaadin-input-field-value-color`                       |\n| `--vaadin-input-field-value-font-size`                   |\n| `--vaadin-input-field-value-font-weight`                 |\n| `--vaadin-input-field-value-line-height`                 |\n\n### Internal components\n\nIn addition to `<vaadin-date-picker>` itself, the following internal\ncomponents are themable:\n\n- `<vaadin-date-picker-overlay-content>`\n- `<vaadin-date-picker-month-scroller>`\n- `<vaadin-date-picker-year-scroller>`\n- `<vaadin-date-picker-year>`\n- `<vaadin-month-calendar>`\n\nIn order to style the overlay content, use `<vaadin-date-picker-overlay-content>` shadow DOM parts:\n\nPart name             | Description\n----------------------|--------------------\n`years-toggle-button` | Fullscreen mode years scroller toggle\n`toolbar`             | Toolbar with slotted buttons\n\nThe following state attributes are available on the `<vaadin-date-picker-overlay-content>` element:\n\nAttribute       | Description\n----------------|-------------------------------------------------\n`desktop`       | Set when the overlay content is in desktop mode\n`fullscreen`    | Set when the overlay content is in fullscreen mode\n`years-visible` | Set when the year scroller is visible in fullscreen mode\n\nIn order to style the month calendar, use `<vaadin-month-calendar>` shadow DOM parts:\n\nPart name             | Description\n----------------------|--------------------\n`month-header`        | Month title\n`weekdays`            | Weekday container\n`weekday`             | Weekday element\n`week-numbers`        | Week numbers container\n`week-number`         | Week number element\n`date`                | Date element\n`disabled`            | Disabled date element\n`focused`             | Focused date element\n`selected`            | Selected date element\n`today`               | Date element corresponding to the current day\n`past`                | Date element corresponding to the date in the past\n`future`              | Date element corresponding to the date in the future\n\nIn order to style year scroller elements, use `<vaadin-date-picker-year>` shadow DOM parts:\n\nPart name             | Description\n----------------------|--------------------\n`year-number`         | Year number\n`year-separator`      | Year separator\n\nSee [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation.\n\n### Change events\n\nDepending on the nature of the value change that the user attempts to commit e.g. by pressing Enter,\nthe component can fire either a `change` event or an `unparsable-change` event:\n\nValue change             | Event\n:------------------------|:------------------\nempty => parsable        | change\nempty => unparsable      | unparsable-change\nparsable => empty        | change\nparsable => parsable     | change\nparsable => unparsable   | change\nunparsable => empty      | unparsable-change\nunparsable => parsable   | change\nunparsable => unparsable | unparsable-change",
        props=(
            PropertySchema(
                name="accessible-name",
                kind="string",
                choices=(),
                default=None,
                description="String used to label the component to screen reader users.",
            ),
            PropertySchema(
                name="accessible-name-ref",
                kind="string",
                choices=(),
                default=None,
                description="Id of the element used as label of the component to screen reader users.",
            ),
            PropertySchema(
                name="allowed-char-pattern",
                kind="string",
                choices=(),
                default=None,
                description="A pattern matched against individual characters the user inputs.\n\nWhen set, the field will prevent:\n- `keydown` events if the entered key doesn't match `/^allowedCharPattern$/`\n- `paste` events if the pasted text doesn't match `/^allowedCharPattern*$/`\n- `drop` events if the dropped text doesn't match `/^allowedCharPattern*$/`\n\nFor example, to allow entering only numbers and minus signs, use:\n`allowedCharPattern = \"[\\\\d-]\"`",
            ),
            PropertySchema(
                name="auto-open-disabled",
                kind="boolean",
                choices=(),
                default=None,
                description="Set true to prevent the overlay from opening automatically.",
            ),
            PropertySchema(
                name="autofocus",
                kind="boolean",
                choices=(),
                default=None,
                description="Specify that this control should have input focus when the page loads.",
            ),
            PropertySchema(
                name="autoselect",
                kind="boolean",
                choices=(),
                default=None,
                description="If true, the input text gets fully selected when the field is focused using click or touch / tap.",
            ),
            PropertySchema(
                name="clear-button-visible",
                kind="boolean",
                choices=(),
                default=None,
                description="Set to true to display the clear icon which clears the input.\nThis also enables clearing the input when pressing Esc key.",
            ),
            PropertySchema(
                name="disabled", kind="boolean", choices=(), default=None, description="If true, the user cannot interact with this element."
            ),
            PropertySchema(name="error-message", kind="string", choices=(), default=None, description="Error to show when the field is invalid."),
            PropertySchema(name="helper-text", kind="string", choices=(), default=None, description="String used for the helper text."),
            PropertySchema(
                name="i18n",
                kind="json",
                choices=(),
                type_text="Object",
                default=None,
                description="The object used to localize this component. To change the default\nlocalization, replace this with an object that provides all properties, or\njust the individual properties you want to change.\n\nShould be overridden by subclasses to provide a custom JSDoc with the\ndefault I18N properties.",
            ),
            PropertySchema(
                name="initial-position",
                kind="string",
                choices=(),
                default=None,
                description="Date which should be visible when there is no value selected.\n\nThe same date formats as for the `value` property are supported.",
            ),
            PropertySchema(name="invalid", kind="boolean", choices=(), default=None, description="Set to true when the field is invalid."),
            PropertySchema(
                name="is-date-disabled",
                kind="json",
                choices=(),
                type_text="function(DatePickerDate): boolean | undefined",
                default=None,
                description="A function to be used to determine whether the user can select a given date.\nReceives a `DatePickerDate` object of the date to be selected and should return a\nboolean.",
            ),
            PropertySchema(
                name="label",
                kind="string",
                choices=(),
                default=None,
                description="The label text for the input node.\nWhen no light dom defined via [slot=label], this value will be used.",
            ),
            PropertySchema(
                name="manual-validation",
                kind="boolean",
                choices=(),
                default=None,
                description="Set to true to enable manual validation mode. This mode disables automatic\nconstraint validation, allowing you to control the validation process yourself.\nYou can still trigger constraint validation manually with the `validate()` method\nor use `checkValidity()` to assess the component's validity without affecting\nthe invalid state. In manual validation mode, you can also manipulate\nthe `invalid` property directly through your application logic without conflicts\nwith the component's internal validation.",
            ),
            PropertySchema(
                name="max",
                kind="string",
                choices=(),
                default=None,
                description='The latest date that can be selected. All later dates will be disabled.\n\nSupported date formats:\n- ISO 8601 `"YYYY-MM-DD"` (default)\n- 6-digit extended ISO 8601 `"+YYYYYY-MM-DD"`, `"-YYYYYY-MM-DD"`',
            ),
            PropertySchema(
                name="min",
                kind="string",
                choices=(),
                default=None,
                description='The earliest date that can be selected. All earlier dates will be disabled.\n\nSupported date formats:\n- ISO 8601 `"YYYY-MM-DD"` (default)\n- 6-digit extended ISO 8601 `"+YYYYYY-MM-DD"`, `"-YYYYYY-MM-DD"`',
            ),
            PropertySchema(name="name", kind="string", choices=(), default=None, description="The name of this field."),
            PropertySchema(name="opened", kind="boolean", choices=(), default=None, description="Set true to open the date selector overlay."),
            PropertySchema(
                name="placeholder", kind="string", choices=(), default=None, description="A hint to the user of what can be entered in the field."
            ),
            PropertySchema(
                name="readonly", kind="boolean", choices=(), default=None, description="When present, it specifies that the field is read-only."
            ),
            PropertySchema(name="required", kind="boolean", choices=(), default=None, description="Specifies that the user must fill in a value."),
            PropertySchema(
                name="show-week-numbers",
                kind="boolean",
                choices=(),
                default=None,
                description="Set true to display ISO-8601 week numbers in the calendar. Notice that\ndisplaying week numbers is only supported when `i18n.firstDayOfWeek`\nis 1 (Monday).",
            ),
            PropertySchema(
                name="title",
                kind="string",
                choices=(),
                default=None,
                description="The text usually displayed in a tooltip popup when the mouse is over the field.",
            ),
            PropertySchema(
                name="value",
                kind="string",
                choices=(),
                default=None,
                description='Selected date.\n\nSupported date formats:\n- ISO 8601 `"YYYY-MM-DD"` (default)\n- 6-digit extended ISO 8601 `"+YYYYYY-MM-DD"`, `"-YYYYYY-MM-DD"`',
            ),
            PropertySchema(
                name="theme",
                kind="string",
                choices=(),
                default=None,
                description="Space-separated theme variants, such as `primary` or `small`, which the element propagates to its parts.",
            ),
        ),
        events=("change", "invalid-changed", "opened-changed", "unparsable-change", "validated", "value-changed"),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        accessible_name: str | None = None,
        accessible_name_ref: str | None = None,
        allowed_char_pattern: str | None = None,
        auto_open_disabled: bool | None = None,
        autofocus: bool | None = None,
        autoselect: bool | None = None,
        clear_button_visible: bool | None = None,
        disabled: bool | None = None,
        error_message: str | None = None,
        helper_text: str | None = None,
        i18n: Any = None,
        initial_position: str | None = None,
        invalid: bool | None = None,
        is_date_disabled: Any = None,
        label: str | None = None,
        manual_validation: bool | None = None,
        max: str | None = None,
        min: str | None = None,
        name: str | None = None,
        opened: bool | None = None,
        placeholder: str | None = None,
        readonly: bool | None = None,
        required: bool | None = None,
        show_week_numbers: bool | None = None,
        title: str | None = None,
        value: str | None = None,
        theme: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "accessible-name": accessible_name,
                "accessible-name-ref": accessible_name_ref,
                "allowed-char-pattern": allowed_char_pattern,
                "auto-open-disabled": auto_open_disabled,
                "autofocus": autofocus,
                "autoselect": autoselect,
                "clear-button-visible": clear_button_visible,
                "disabled": disabled,
                "error-message": error_message,
                "helper-text": helper_text,
                "i18n": i18n,
                "initial-position": initial_position,
                "invalid": invalid,
                "is-date-disabled": is_date_disabled,
                "label": label,
                "manual-validation": manual_validation,
                "max": max,
                "min": min,
                "name": name,
                "opened": opened,
                "placeholder": placeholder,
                "readonly": readonly,
                "required": required,
                "show-week-numbers": show_week_numbers,
                "title": title,
                "value": value,
                "theme": theme,
            },
            **props,
        )


class VaadinDialog(Component):
    """`<vaadin-dialog>` is a Web Component for creating customized modal dialogs. ### Rendering The content of the dialog can be populated by using the renderer callback function. The renderer function provides `root`, `dialog` arguments. Generate DOM content, append it to the `root` element and control the state of the host element by accessing `dialog`. Before generating new content, users are able to check if there is already content in `root` for reusing it. ```html <vaadin-dialog id="dialog"></vaadin-dialog> ``` ```js const dialog = document.querySelector('#dialog'); dialog.renderer = function(root, dialog) { root.textContent = "Sample dialog"; }; ``` Renderer is called on the opening of the dialog. DOM generated during the renderer call can be reused in the next renderer call and will be provided with the `root` argument. On first call it will be empty. ### Styling The following shadow DOM parts are available for styling: Part name | Description -----------------|------------------------------------------- `backdrop` | Backdrop of the overlay `overlay` | The overlay container `content` | The overlay content `header` | Element wrapping title and header content `header-content` | Element wrapping the header content slot `title` | Element wrapping the title slot `footer` | Element wrapping the footer slot The following state attributes are available for styling: Attribute | Description -----------------|-------------------------------------------- `has-title` | Set when the element has a title `has-header` | Set when the element has header renderer `has-footer` | Set when the element has footer renderer `overflow` | Set to `top`, `bottom`, none or both The following custom CSS properties are available for styling: Custom CSS property | :--------------------------------------------| |`--vaadin-dialog-background` | |`--vaadin-dialog-border-color` | |`--vaadin-dialog-border-radius` | |`--vaadin-dialog-border-width` | |`--vaadin-dialog-max-width` | |`--vaadin-dialog-min-width` | |`--vaadin-dialog-overflow-indicator-color` | |`--vaadin-dialog-overflow-indicator-height` | |`--vaadin-dialog-padding` | |`--vaadin-dialog-shadow` | |`--vaadin-dialog-text-color` | |`--vaadin-dialog-title-color` | |`--vaadin-dialog-title-font-size` | |`--vaadin-dialog-title-font-weight` | |`--vaadin-dialog-title-line-height` | |`--vaadin-dialog-toolbar-gap` | |`--vaadin-overlay-backdrop-background` | See [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation."""

    tag = "vaadin-dialog"
    schema = ComponentSchema(
        tag="vaadin-dialog",
        class_name="VaadinDialog",
        summary='`<vaadin-dialog>` is a Web Component for creating customized modal dialogs.\n\n### Rendering\n\nThe content of the dialog can be populated by using the renderer callback function.\n\nThe renderer function provides `root`, `dialog` arguments.\nGenerate DOM content, append it to the `root` element and control the state\nof the host element by accessing `dialog`. Before generating new content,\nusers are able to check if there is already content in `root` for reusing it.\n\n```html\n<vaadin-dialog id="dialog"></vaadin-dialog>\n```\n```js\nconst dialog = document.querySelector(\'#dialog\');\ndialog.renderer = function(root, dialog) {\n  root.textContent = "Sample dialog";\n};\n```\n\nRenderer is called on the opening of the dialog.\nDOM generated during the renderer call can be reused\nin the next renderer call and will be provided with the `root` argument.\nOn first call it will be empty.\n\n### Styling\n\nThe following shadow DOM parts are available for styling:\n\nPart name        | Description\n-----------------|-------------------------------------------\n`backdrop`       | Backdrop of the overlay\n`overlay`        | The overlay container\n`content`        | The overlay content\n`header`         | Element wrapping title and header content\n`header-content` | Element wrapping the header content slot\n`title`          | Element wrapping the title slot\n`footer`         | Element wrapping the footer slot\n\nThe following state attributes are available for styling:\n\nAttribute        | Description\n-----------------|--------------------------------------------\n`has-title`      | Set when the element has a title\n`has-header`     | Set when the element has header renderer\n`has-footer`     | Set when the element has footer renderer\n`overflow`       | Set to `top`, `bottom`, none or both\n\nThe following custom CSS properties are available for styling:\n\nCustom CSS property                          |\n:--------------------------------------------|\n|`--vaadin-dialog-background`                |\n|`--vaadin-dialog-border-color`              |\n|`--vaadin-dialog-border-radius`             |\n|`--vaadin-dialog-border-width`              |\n|`--vaadin-dialog-max-width`                 |\n|`--vaadin-dialog-min-width`                 |\n|`--vaadin-dialog-overflow-indicator-color`  |\n|`--vaadin-dialog-overflow-indicator-height` |\n|`--vaadin-dialog-padding`                   |\n|`--vaadin-dialog-shadow`                    |\n|`--vaadin-dialog-text-color`                |\n|`--vaadin-dialog-title-color`               |\n|`--vaadin-dialog-title-font-size`           |\n|`--vaadin-dialog-title-font-weight`         |\n|`--vaadin-dialog-title-line-height`         |\n|`--vaadin-dialog-toolbar-gap`               |\n|`--vaadin-overlay-backdrop-background`      |\n\nSee [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation.',
        props=(
            PropertySchema(
                name="draggable",
                kind="boolean",
                choices=(),
                default=None,
                description='Set to true to enable repositioning the dialog by clicking and dragging.\n\nBy default, only the overlay area can be used to drag the element. But,\na child element can be marked as a draggable area by adding a\n"`draggable`" class to it, this will by default make all of its children draggable also.\nIf you want a child element to be draggable\nbut still have its children non-draggable (by default), mark it with\n"`draggable-leaf-only`" class name.',
            ),
            PropertySchema(
                name="footer-renderer",
                kind="json",
                choices=(),
                type_text="DialogRenderer | undefined",
                default=None,
                description="Custom function for rendering the dialog footer.\nReceives two arguments:\n\n- `root` The root container DOM element. Append your content to it.\n- `dialog` The reference to the `<vaadin-dialog>` element.\n\nWhen `footerRenderer` is set, the attribute `has-footer` is set on the dialog.",
            ),
            PropertySchema(
                name="header-renderer",
                kind="json",
                choices=(),
                type_text="DialogRenderer | undefined",
                default=None,
                description="Custom function for rendering the dialog header.\nReceives two arguments:\n\n- `root` The root container DOM element. Append your content to it.\n- `dialog` The reference to the `<vaadin-dialog>` element.\n\nIf both `headerTitle` and `headerRenderer` are defined, the title\nand the elements created by the renderer will be placed next to\neach other, with the title coming first.\n\nWhen `headerRenderer` is set, the attribute `has-header` is set on the dialog.",
            ),
            PropertySchema(
                name="header-title",
                kind="string",
                choices=(),
                default=None,
                description="String used for rendering a dialog title.\n\nIf both `headerTitle` and `headerRenderer` are defined, the title\nand the elements created by the renderer will be placed next to\neach other, with the title coming first.\n\nWhen `headerTitle` is set, the attribute `has-title` is set on the dialog.",
            ),
            PropertySchema(
                name="height",
                kind="string",
                choices=(),
                default=None,
                description="Set the height of the dialog.\nIf a unitless number is provided, pixels are assumed.",
            ),
            PropertySchema(
                name="keep-in-viewport",
                kind="boolean",
                choices=(),
                default=None,
                description="Set to true to prevent the dialog from moving outside the viewport bounds.\nWhen enabled, all four edges of the dialog will remain visible, for example\nwhen dragging the dialog or when the viewport is resized. Note that the\ndialog will also adjust any programmatically configured size and position\nso that it stays within the viewport.",
            ),
            PropertySchema(
                name="left",
                kind="string",
                choices=(),
                default=None,
                description="Set the distance of the dialog from the left of the viewport.\nIf a unitless number is provided, pixels are assumed.\n\nNote that the dialog uses an internal container that has some\nadditional spacing, which can be overridden by the theme.",
            ),
            PropertySchema(
                name="modeless",
                kind="boolean",
                choices=(),
                default=None,
                description="Set to true to remove backdrop and allow click events on background elements.",
            ),
            PropertySchema(
                name="no-close-on-esc", kind="boolean", choices=(), default=None, description="Set to true to disable closing dialog on Escape press"
            ),
            PropertySchema(
                name="no-close-on-outside-click",
                kind="boolean",
                choices=(),
                default=None,
                description="Set to true to disable closing dialog on outside click",
            ),
            PropertySchema(name="no-focus-trap", kind="boolean", choices=(), default=None, description="Set to true to disable focus trapping."),
            PropertySchema(
                name="opened", kind="boolean", choices=(), default=None, description="True if the dialog is visible and available for interaction."
            ),
            PropertySchema(
                name="overlay-role",
                kind="string",
                choices=(),
                default=None,
                description='The `role` attribute value to be set on the dialog. Defaults to "dialog".',
            ),
            PropertySchema(
                name="renderer",
                kind="json",
                choices=(),
                type_text="DialogRenderer | undefined",
                default=None,
                description="Custom function for rendering the content of the dialog.\nReceives two arguments:\n\n- `root` The root container DOM element. Append your content to it.\n- `dialog` The reference to the `<vaadin-dialog>` element.",
            ),
            PropertySchema(
                name="resizable",
                kind="boolean",
                choices=(),
                default=None,
                description="Set to true to enable resizing the dialog by dragging the corners and edges.",
            ),
            PropertySchema(
                name="top",
                kind="string",
                choices=(),
                default=None,
                description="Set the distance of the dialog from the top of the viewport.\nIf a unitless number is provided, pixels are assumed.\n\nNote that the dialog uses an internal container that has some\nadditional spacing, which can be overridden by the theme.",
            ),
            PropertySchema(
                name="width",
                kind="string",
                choices=(),
                default=None,
                description="Set the width of the dialog.\nIf a unitless number is provided, pixels are assumed.",
            ),
            PropertySchema(
                name="theme",
                kind="string",
                choices=(),
                default=None,
                description="Space-separated theme variants, such as `primary` or `small`, which the element propagates to its parts.",
            ),
        ),
        events=("closed", "drag-start", "dragged", "opened-changed", "resize", "resize-start"),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        draggable: bool | None = None,
        footer_renderer: Any = None,
        header_renderer: Any = None,
        header_title: str | None = None,
        height: str | None = None,
        keep_in_viewport: bool | None = None,
        left: str | None = None,
        modeless: bool | None = None,
        no_close_on_esc: bool | None = None,
        no_close_on_outside_click: bool | None = None,
        no_focus_trap: bool | None = None,
        opened: bool | None = None,
        overlay_role: str | None = None,
        renderer: Any = None,
        resizable: bool | None = None,
        top: str | None = None,
        width: str | None = None,
        theme: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "draggable": draggable,
                "footer-renderer": footer_renderer,
                "header-renderer": header_renderer,
                "header-title": header_title,
                "height": height,
                "keep-in-viewport": keep_in_viewport,
                "left": left,
                "modeless": modeless,
                "no-close-on-esc": no_close_on_esc,
                "no-close-on-outside-click": no_close_on_outside_click,
                "no-focus-trap": no_focus_trap,
                "opened": opened,
                "overlay-role": overlay_role,
                "renderer": renderer,
                "resizable": resizable,
                "top": top,
                "width": width,
                "theme": theme,
            },
            **props,
        )


class VaadinGridColumnGroup(Component):
    """A `<vaadin-grid-column-group>` is used to make groups of columns in `<vaadin-grid>` and to configure additional headers and footers. Groups can be nested to create complex header and footer configurations. #### Example: ```html <vaadin-grid-column-group resizable id="columnGroup"> <vaadin-grid-column id="column1"></vaadin-grid-column> <vaadin-grid-column id="column2"></vaadin-grid-column> </vaadin-grid-column-group> ``` ```js const columnGroup = document.querySelector('#columnGroup'); columnGroup.headerRenderer = (root, columnGroup) => { root.textContent = 'header'; } const column1 = document.querySelector('#column1'); column1.headerRenderer = (root, column) => { ... }; column1.renderer = (root, column, model) => { ... }; const column2 = document.querySelector('#column2'); column2.headerRenderer = (root, column) => { ... }; column2.renderer = (root, column, model) => { ... }; ```"""

    tag = "vaadin-grid-column-group"
    schema = ComponentSchema(
        tag="vaadin-grid-column-group",
        class_name="VaadinGridColumnGroup",
        summary="A `<vaadin-grid-column-group>` is used to make groups of columns in `<vaadin-grid>` and\nto configure additional headers and footers.\n\nGroups can be nested to create complex header and footer configurations.\n\n#### Example:\n```html\n<vaadin-grid-column-group resizable id=\"columnGroup\">\n  <vaadin-grid-column id=\"column1\"></vaadin-grid-column>\n  <vaadin-grid-column id=\"column2\"></vaadin-grid-column>\n</vaadin-grid-column-group>\n```\n\n```js\nconst columnGroup = document.querySelector('#columnGroup');\ncolumnGroup.headerRenderer = (root, columnGroup) => {\n  root.textContent = 'header';\n}\n\nconst column1 = document.querySelector('#column1');\ncolumn1.headerRenderer = (root, column) => { ... };\ncolumn1.renderer = (root, column, model) => { ... };\n\nconst column2 = document.querySelector('#column2');\ncolumn2.headerRenderer = (root, column) => { ... };\ncolumn2.renderer = (root, column, model) => { ... };\n```",
        props=(
            PropertySchema(
                name="flex-grow",
                kind="number",
                choices=(),
                default=None,
                description="Flex grow ratio for the column group as the sum of the ratios of its child columns.",
            ),
            PropertySchema(name="footer-part-name", kind="string", choices=(), default=None, description="Custom part name for the footer cell."),
            PropertySchema(
                name="footer-renderer",
                kind="json",
                choices=(),
                type_text="GridHeaderFooterRenderer | null | undefined",
                default=None,
                description="Custom function for rendering the footer content.\nReceives two arguments:\n\n- `root` The footer cell content DOM element. Append your content to it.\n- `column` The `<vaadin-grid-column>` element.",
            ),
            PropertySchema(
                name="frozen",
                kind="boolean",
                choices=(),
                default=None,
                description="When true, the column is frozen. When a column inside of a column group is frozen,\nall of the sibling columns inside the group will get frozen also.",
            ),
            PropertySchema(
                name="frozen-to-end",
                kind="boolean",
                choices=(),
                default=None,
                description="When true, the column is frozen to end of grid.\n\nWhen a column inside of a column group is frozen to end, all of the sibling columns\ninside the group will get frozen to end also.\n\nColumn can not be set as `frozen` and `frozenToEnd` at the same time.",
            ),
            PropertySchema(
                name="header", kind="string", choices=(), default=None, description="Text content to display in the header cell of the column."
            ),
            PropertySchema(name="header-part-name", kind="string", choices=(), default=None, description="Custom part name for the header cell."),
            PropertySchema(
                name="header-renderer",
                kind="json",
                choices=(),
                type_text="GridHeaderFooterRenderer | null | undefined",
                default=None,
                description="Custom function for rendering the header content.\nReceives two arguments:\n\n- `root` The header cell content DOM element. Append your content to it.\n- `column` The `<vaadin-grid-column>` element.",
            ),
            PropertySchema(
                name="hidden", kind="boolean", choices=(), default=None, description="When set to true, the cells for this column are hidden."
            ),
            PropertySchema(
                name="resizable", kind="boolean", choices=(), default="false", description="When set to true, the column is user-resizable."
            ),
            PropertySchema(
                name="row-header",
                kind="boolean",
                choices=(),
                default=None,
                description="When true, the cells for this column will be rendered with the `role` attribute\nset as `rowheader`, instead of the `gridcell` role value used by default.\n\nWhen a column is set as row header, its cells will be announced by screen readers\nwhile navigating to help user identify the current row as uniquely as possible.",
            ),
            PropertySchema(
                name="text-align",
                kind="string",
                choices=(),
                default=None,
                description='Aligns the columns cell content horizontally.\nSupported values: "start", "center" and "end".',
            ),
            PropertySchema(
                name="width",
                kind="string",
                choices=(),
                default=None,
                description="Width of the column group as the sum of the widths of its child columns.",
            ),
        ),
        events=(),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        flex_grow: float | None = None,
        footer_part_name: str | None = None,
        footer_renderer: Any = None,
        frozen: bool | None = None,
        frozen_to_end: bool | None = None,
        header: str | None = None,
        header_part_name: str | None = None,
        header_renderer: Any = None,
        hidden: bool | None = None,
        resizable: bool | None = None,
        row_header: bool | None = None,
        text_align: str | None = None,
        width: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "flex-grow": flex_grow,
                "footer-part-name": footer_part_name,
                "footer-renderer": footer_renderer,
                "frozen": frozen,
                "frozen-to-end": frozen_to_end,
                "header": header,
                "header-part-name": header_part_name,
                "header-renderer": header_renderer,
                "hidden": hidden,
                "resizable": resizable,
                "row-header": row_header,
                "text-align": text_align,
                "width": width,
            },
            **props,
        )


class VaadinGridColumn(Component):
    """A `<vaadin-grid-column>` is used to configure how a column in `<vaadin-grid>` should look like. See [`<vaadin-grid>`](#/elements/vaadin-grid) documentation for instructions on how to configure the `<vaadin-grid-column>`."""

    tag = "vaadin-grid-column"
    schema = ComponentSchema(
        tag="vaadin-grid-column",
        class_name="VaadinGridColumn",
        summary="A `<vaadin-grid-column>` is used to configure how a column in `<vaadin-grid>`\nshould look like.\n\nSee [`<vaadin-grid>`](#/elements/vaadin-grid) documentation for instructions on how\nto configure the `<vaadin-grid-column>`.",
        props=(
            PropertySchema(
                name="auto-width",
                kind="boolean",
                choices=(),
                default=None,
                description="Automatically sets the width of the column based on the column contents when this is set to `true`.\n\nFor performance reasons the column width is calculated automatically only once when the grid items\nare rendered for the first time and the calculation only considers the rows which are currently\nrendered in DOM (a bit more than what is currently visible). If the grid is scrolled, or the cell\ncontent changes, the column width might not match the contents anymore.\n\nHidden columns are ignored in the calculation and their widths are not automatically updated when\nyou show a column that was initially hidden.\n\nYou can manually trigger the auto sizing behavior again by calling `grid.recalculateColumnWidths()`.\n\nThe column width may still grow larger when `flexGrow` is not 0.",
            ),
            PropertySchema(
                name="flex-grow",
                kind="number",
                choices=(),
                default=None,
                description="Flex grow ratio for the cell widths. When set to 0, cell width is fixed.",
            ),
            PropertySchema(name="footer-part-name", kind="string", choices=(), default=None, description="Custom part name for the footer cell."),
            PropertySchema(
                name="footer-renderer",
                kind="json",
                choices=(),
                type_text="GridHeaderFooterRenderer | null | undefined",
                default=None,
                description="Custom function for rendering the footer content.\nReceives two arguments:\n\n- `root` The footer cell content DOM element. Append your content to it.\n- `column` The `<vaadin-grid-column>` element.",
            ),
            PropertySchema(
                name="frozen",
                kind="boolean",
                choices=(),
                default=None,
                description="When true, the column is frozen. When a column inside of a column group is frozen,\nall of the sibling columns inside the group will get frozen also.",
            ),
            PropertySchema(
                name="frozen-to-end",
                kind="boolean",
                choices=(),
                default=None,
                description="When true, the column is frozen to end of grid.\n\nWhen a column inside of a column group is frozen to end, all of the sibling columns\ninside the group will get frozen to end also.\n\nColumn can not be set as `frozen` and `frozenToEnd` at the same time.",
            ),
            PropertySchema(
                name="header", kind="string", choices=(), default=None, description="Text content to display in the header cell of the column."
            ),
            PropertySchema(name="header-part-name", kind="string", choices=(), default=None, description="Custom part name for the header cell."),
            PropertySchema(
                name="header-renderer",
                kind="json",
                choices=(),
                type_text="GridHeaderFooterRenderer | null | undefined",
                default=None,
                description="Custom function for rendering the header content.\nReceives two arguments:\n\n- `root` The header cell content DOM element. Append your content to it.\n- `column` The `<vaadin-grid-column>` element.",
            ),
            PropertySchema(
                name="hidden", kind="boolean", choices=(), default=None, description="When set to true, the cells for this column are hidden."
            ),
            PropertySchema(
                name="path",
                kind="string",
                choices=(),
                default=None,
                description="Path to an item sub-property whose value gets displayed in the column body cells.\nThe property name is also shown in the column header if an explicit header or renderer isn't defined.",
            ),
            PropertySchema(
                name="renderer",
                kind="json",
                choices=(),
                type_text="GridBodyRenderer | null | undefined",
                default=None,
                description="Custom function for rendering the cell content.\nReceives three arguments:\n\n- `root` The cell content DOM element. Append your content to it.\n- `column` The `<vaadin-grid-column>` element.\n- `model` The object with the properties related with\n  the rendered item, contains:\n  - `model.index` The index of the item.\n  - `model.item` The item.\n  - `model.expanded` Sublevel toggle state.\n  - `model.level` Level of the tree represented with a horizontal offset of the toggle button.\n  - `model.selected` Selected state.\n  - `model.detailsOpened` Details opened state.\n  - `model.hasChildren` Whether the item has children.",
            ),
            PropertySchema(
                name="resizable", kind="boolean", choices=(), default="false", description="When set to true, the column is user-resizable."
            ),
            PropertySchema(
                name="row-header",
                kind="boolean",
                choices=(),
                default=None,
                description="When true, the cells for this column will be rendered with the `role` attribute\nset as `rowheader`, instead of the `gridcell` role value used by default.\n\nWhen a column is set as row header, its cells will be announced by screen readers\nwhile navigating to help user identify the current row as uniquely as possible.",
            ),
            PropertySchema(
                name="text-align",
                kind="string",
                choices=(),
                default=None,
                description='Aligns the columns cell content horizontally.\nSupported values: "start", "center" and "end".',
            ),
            PropertySchema(
                name="width",
                kind="string",
                choices=(),
                default=None,
                description="Width of the cells for this column.\n\nPlease note that using the `em` length unit is discouraged as\nit might lead to misalignment issues if the header, body, and footer\ncells have different font sizes. Instead, use `rem` if you need\na length unit relative to the font size.",
            ),
        ),
        events=(),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        auto_width: bool | None = None,
        flex_grow: float | None = None,
        footer_part_name: str | None = None,
        footer_renderer: Any = None,
        frozen: bool | None = None,
        frozen_to_end: bool | None = None,
        header: str | None = None,
        header_part_name: str | None = None,
        header_renderer: Any = None,
        hidden: bool | None = None,
        path: str | None = None,
        renderer: Any = None,
        resizable: bool | None = None,
        row_header: bool | None = None,
        text_align: str | None = None,
        width: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "auto-width": auto_width,
                "flex-grow": flex_grow,
                "footer-part-name": footer_part_name,
                "footer-renderer": footer_renderer,
                "frozen": frozen,
                "frozen-to-end": frozen_to_end,
                "header": header,
                "header-part-name": header_part_name,
                "header-renderer": header_renderer,
                "hidden": hidden,
                "path": path,
                "renderer": renderer,
                "resizable": resizable,
                "row-header": row_header,
                "text-align": text_align,
                "width": width,
            },
            **props,
        )


class VaadinGridFilterColumn(Component):
    """`<vaadin-grid-filter-column>` is a helper element for the `<vaadin-grid>` that provides default header renderer and functionality for filtering. #### Example: ```html <vaadin-grid> <vaadin-grid-filter-column path="name.first"></vaadin-grid-filter-column> <vaadin-grid-column> ... ```"""

    tag = "vaadin-grid-filter-column"
    schema = ComponentSchema(
        tag="vaadin-grid-filter-column",
        class_name="VaadinGridFilterColumn",
        summary='`<vaadin-grid-filter-column>` is a helper element for the `<vaadin-grid>`\nthat provides default header renderer and functionality for filtering.\n\n#### Example:\n```html\n<vaadin-grid>\n <vaadin-grid-filter-column path="name.first"></vaadin-grid-filter-column>\n\n <vaadin-grid-column>\n   ...\n```',
        props=(
            PropertySchema(
                name="auto-width",
                kind="boolean",
                choices=(),
                default=None,
                description="Automatically sets the width of the column based on the column contents when this is set to `true`.\n\nFor performance reasons the column width is calculated automatically only once when the grid items\nare rendered for the first time and the calculation only considers the rows which are currently\nrendered in DOM (a bit more than what is currently visible). If the grid is scrolled, or the cell\ncontent changes, the column width might not match the contents anymore.\n\nHidden columns are ignored in the calculation and their widths are not automatically updated when\nyou show a column that was initially hidden.\n\nYou can manually trigger the auto sizing behavior again by calling `grid.recalculateColumnWidths()`.\n\nThe column width may still grow larger when `flexGrow` is not 0.",
            ),
            PropertySchema(
                name="flex-grow",
                kind="number",
                choices=(),
                default=None,
                description="Flex grow ratio for the cell widths. When set to 0, cell width is fixed.",
            ),
            PropertySchema(name="footer-part-name", kind="string", choices=(), default=None, description="Custom part name for the footer cell."),
            PropertySchema(
                name="footer-renderer",
                kind="json",
                choices=(),
                type_text="GridHeaderFooterRenderer | null | undefined",
                default=None,
                description="Custom function for rendering the footer content.\nReceives two arguments:\n\n- `root` The footer cell content DOM element. Append your content to it.\n- `column` The `<vaadin-grid-column>` element.",
            ),
            PropertySchema(
                name="frozen",
                kind="boolean",
                choices=(),
                default=None,
                description="When true, the column is frozen. When a column inside of a column group is frozen,\nall of the sibling columns inside the group will get frozen also.",
            ),
            PropertySchema(
                name="frozen-to-end",
                kind="boolean",
                choices=(),
                default=None,
                description="When true, the column is frozen to end of grid.\n\nWhen a column inside of a column group is frozen to end, all of the sibling columns\ninside the group will get frozen to end also.\n\nColumn can not be set as `frozen` and `frozenToEnd` at the same time.",
            ),
            PropertySchema(
                name="header", kind="string", choices=(), default=None, description="Text to display as the label of the column filter text-field."
            ),
            PropertySchema(name="header-part-name", kind="string", choices=(), default=None, description="Custom part name for the header cell."),
            PropertySchema(
                name="header-renderer",
                kind="json",
                choices=(),
                type_text="GridHeaderFooterRenderer | null | undefined",
                default=None,
                description="Custom function for rendering the header content.\nReceives two arguments:\n\n- `root` The header cell content DOM element. Append your content to it.\n- `column` The `<vaadin-grid-column>` element.",
            ),
            PropertySchema(
                name="hidden", kind="boolean", choices=(), default=None, description="When set to true, the cells for this column are hidden."
            ),
            PropertySchema(
                name="path", kind="string", choices=(), default=None, description="JS Path of the property in the item used for filtering the data."
            ),
            PropertySchema(
                name="renderer",
                kind="json",
                choices=(),
                type_text="GridBodyRenderer | null | undefined",
                default=None,
                description="Custom function for rendering the cell content.\nReceives three arguments:\n\n- `root` The cell content DOM element. Append your content to it.\n- `column` The `<vaadin-grid-column>` element.\n- `model` The object with the properties related with\n  the rendered item, contains:\n  - `model.index` The index of the item.\n  - `model.item` The item.\n  - `model.expanded` Sublevel toggle state.\n  - `model.level` Level of the tree represented with a horizontal offset of the toggle button.\n  - `model.selected` Selected state.\n  - `model.detailsOpened` Details opened state.\n  - `model.hasChildren` Whether the item has children.",
            ),
            PropertySchema(
                name="resizable", kind="boolean", choices=(), default="false", description="When set to true, the column is user-resizable."
            ),
            PropertySchema(
                name="row-header",
                kind="boolean",
                choices=(),
                default=None,
                description="When true, the cells for this column will be rendered with the `role` attribute\nset as `rowheader`, instead of the `gridcell` role value used by default.\n\nWhen a column is set as row header, its cells will be announced by screen readers\nwhile navigating to help user identify the current row as uniquely as possible.",
            ),
            PropertySchema(
                name="text-align",
                kind="string",
                choices=(),
                default=None,
                description='Aligns the columns cell content horizontally.\nSupported values: "start", "center" and "end".',
            ),
            PropertySchema(
                name="width",
                kind="string",
                choices=(),
                default=None,
                description="Width of the cells for this column.\n\nPlease note that using the `em` length unit is discouraged as\nit might lead to misalignment issues if the header, body, and footer\ncells have different font sizes. Instead, use `rem` if you need\na length unit relative to the font size.",
            ),
        ),
        events=(),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        auto_width: bool | None = None,
        flex_grow: float | None = None,
        footer_part_name: str | None = None,
        footer_renderer: Any = None,
        frozen: bool | None = None,
        frozen_to_end: bool | None = None,
        header: str | None = None,
        header_part_name: str | None = None,
        header_renderer: Any = None,
        hidden: bool | None = None,
        path: str | None = None,
        renderer: Any = None,
        resizable: bool | None = None,
        row_header: bool | None = None,
        text_align: str | None = None,
        width: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "auto-width": auto_width,
                "flex-grow": flex_grow,
                "footer-part-name": footer_part_name,
                "footer-renderer": footer_renderer,
                "frozen": frozen,
                "frozen-to-end": frozen_to_end,
                "header": header,
                "header-part-name": header_part_name,
                "header-renderer": header_renderer,
                "hidden": hidden,
                "path": path,
                "renderer": renderer,
                "resizable": resizable,
                "row-header": row_header,
                "text-align": text_align,
                "width": width,
            },
            **props,
        )


class VaadinGridFilter(Component):
    """`<vaadin-grid-filter>` is a helper element for the `<vaadin-grid>` that provides out-of-the-box UI controls, and handlers for filtering the grid data. #### Example: ```html <vaadin-grid-column id="column"></vaadin-grid-column> ``` ```js const column = document.querySelector('#column'); column.headerRenderer = (root, column) => { let filter = root.firstElementChild; if (!filter) { filter = document.createElement('vaadin-grid-filter'); root.appendChild(filter); } filter.path = 'name.first'; }; column.renderer = (root, column, model) => { root.textContent = model.item.name.first; }; ```"""

    tag = "vaadin-grid-filter"
    schema = ComponentSchema(
        tag="vaadin-grid-filter",
        class_name="VaadinGridFilter",
        summary="`<vaadin-grid-filter>` is a helper element for the `<vaadin-grid>` that provides out-of-the-box UI controls,\nand handlers for filtering the grid data.\n\n#### Example:\n```html\n<vaadin-grid-column id=\"column\"></vaadin-grid-column>\n```\n```js\nconst column = document.querySelector('#column');\ncolumn.headerRenderer = (root, column) => {\n  let filter = root.firstElementChild;\n  if (!filter) {\n    filter = document.createElement('vaadin-grid-filter');\n    root.appendChild(filter);\n  }\n  filter.path = 'name.first';\n};\ncolumn.renderer = (root, column, model) => {\n  root.textContent = model.item.name.first;\n};\n```",
        props=(
            PropertySchema(
                name="path", kind="string", choices=(), default=None, description="JS Path of the property in the item used for filtering the data."
            ),
            PropertySchema(name="value", kind="string", choices=(), default=None, description="Current filter value."),
            PropertySchema(
                name="theme",
                kind="string",
                choices=(),
                default=None,
                description="Space-separated theme variants, such as `primary` or `small`, which the element propagates to its parts.",
            ),
        ),
        events=("value-changed",),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        path: str | None = None,
        value: str | None = None,
        theme: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "path": path,
                "value": value,
                "theme": theme,
            },
            **props,
        )


class VaadinGridSelectionColumn(Component):
    """`<vaadin-grid-selection-column>` is a helper element for the `<vaadin-grid>` that provides default renderers and functionality for item selection. #### Example: ```html <vaadin-grid> <vaadin-grid-selection-column frozen auto-select></vaadin-grid-selection-column> <vaadin-grid-column> ... ``` By default the selection column displays `<vaadin-checkbox>` elements in the column cells. The checkboxes in the body rows toggle selection of the corresponding row items. When the grid data is provided as an array of [`items`](#/elements/vaadin-grid#property-items), the column header gets an additional checkbox that can be used for toggling selection for all the items at once. __The default content can also be overridden__"""

    tag = "vaadin-grid-selection-column"
    schema = ComponentSchema(
        tag="vaadin-grid-selection-column",
        class_name="VaadinGridSelectionColumn",
        summary="`<vaadin-grid-selection-column>` is a helper element for the `<vaadin-grid>`\nthat provides default renderers and functionality for item selection.\n\n#### Example:\n```html\n<vaadin-grid>\n <vaadin-grid-selection-column frozen auto-select></vaadin-grid-selection-column>\n\n <vaadin-grid-column>\n   ...\n```\n\nBy default the selection column displays `<vaadin-checkbox>` elements in the\ncolumn cells. The checkboxes in the body rows toggle selection of the corresponding row items.\n\nWhen the grid data is provided as an array of [`items`](#/elements/vaadin-grid#property-items),\nthe column header gets an additional checkbox that can be used for toggling\nselection for all the items at once.\n\n__The default content can also be overridden__",
        props=(
            PropertySchema(
                name="auto-select", kind="boolean", choices=(), default=None, description="When true, the active item gets automatically selected."
            ),
            PropertySchema(name="auto-width", kind="boolean", choices=(), default=None, description="Override `autoWidth` to enable auto-width"),
            PropertySchema(
                name="drag-select",
                kind="boolean",
                choices=(),
                default=None,
                description="When true, rows can be selected by dragging over the selection column.",
            ),
            PropertySchema(
                name="flex-grow",
                kind="number",
                choices=(),
                default=None,
                description="Flex grow ratio for the cell widths. When set to 0, cell width is fixed.",
            ),
            PropertySchema(name="footer-part-name", kind="string", choices=(), default=None, description="Custom part name for the footer cell."),
            PropertySchema(
                name="footer-renderer",
                kind="json",
                choices=(),
                type_text="GridHeaderFooterRenderer | null | undefined",
                default=None,
                description="Custom function for rendering the footer content.\nReceives two arguments:\n\n- `root` The footer cell content DOM element. Append your content to it.\n- `column` The `<vaadin-grid-column>` element.",
            ),
            PropertySchema(
                name="frozen",
                kind="boolean",
                choices=(),
                default=None,
                description="When true, the column is frozen. When a column inside of a column group is frozen,\nall of the sibling columns inside the group will get frozen also.",
            ),
            PropertySchema(
                name="frozen-to-end",
                kind="boolean",
                choices=(),
                default=None,
                description="When true, the column is frozen to end of grid.\n\nWhen a column inside of a column group is frozen to end, all of the sibling columns\ninside the group will get frozen to end also.\n\nColumn can not be set as `frozen` and `frozenToEnd` at the same time.",
            ),
            PropertySchema(
                name="header", kind="string", choices=(), default=None, description="Text content to display in the header cell of the column."
            ),
            PropertySchema(name="header-part-name", kind="string", choices=(), default=None, description="Custom part name for the header cell."),
            PropertySchema(
                name="header-renderer",
                kind="json",
                choices=(),
                type_text="GridHeaderFooterRenderer | null | undefined",
                default=None,
                description="Custom function for rendering the header content.\nReceives two arguments:\n\n- `root` The header cell content DOM element. Append your content to it.\n- `column` The `<vaadin-grid-column>` element.",
            ),
            PropertySchema(
                name="hidden", kind="boolean", choices=(), default=None, description="When set to true, the cells for this column are hidden."
            ),
            PropertySchema(
                name="path",
                kind="string",
                choices=(),
                default=None,
                description="Path to an item sub-property whose value gets displayed in the column body cells.\nThe property name is also shown in the column header if an explicit header or renderer isn't defined.",
            ),
            PropertySchema(
                name="renderer",
                kind="json",
                choices=(),
                type_text="GridBodyRenderer | null | undefined",
                default=None,
                description="Custom function for rendering the cell content.\nReceives three arguments:\n\n- `root` The cell content DOM element. Append your content to it.\n- `column` The `<vaadin-grid-column>` element.\n- `model` The object with the properties related with\n  the rendered item, contains:\n  - `model.index` The index of the item.\n  - `model.item` The item.\n  - `model.expanded` Sublevel toggle state.\n  - `model.level` Level of the tree represented with a horizontal offset of the toggle button.\n  - `model.selected` Selected state.\n  - `model.detailsOpened` Details opened state.\n  - `model.hasChildren` Whether the item has children.",
            ),
            PropertySchema(
                name="resizable", kind="boolean", choices=(), default="false", description="When set to true, the column is user-resizable."
            ),
            PropertySchema(
                name="row-header",
                kind="boolean",
                choices=(),
                default=None,
                description="When true, the cells for this column will be rendered with the `role` attribute\nset as `rowheader`, instead of the `gridcell` role value used by default.\n\nWhen a column is set as row header, its cells will be announced by screen readers\nwhile navigating to help user identify the current row as uniquely as possible.",
            ),
            PropertySchema(name="select-all", kind="boolean", choices=(), default=None, description="When true, all the items are selected."),
            PropertySchema(
                name="text-align",
                kind="string",
                choices=(),
                default=None,
                description='Aligns the columns cell content horizontally.\nSupported values: "start", "center" and "end".',
            ),
            PropertySchema(name="width", kind="string", choices=(), default=None, description="Width of the cells for this column."),
        ),
        events=("select-all-changed",),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        auto_select: bool | None = None,
        auto_width: bool | None = None,
        drag_select: bool | None = None,
        flex_grow: float | None = None,
        footer_part_name: str | None = None,
        footer_renderer: Any = None,
        frozen: bool | None = None,
        frozen_to_end: bool | None = None,
        header: str | None = None,
        header_part_name: str | None = None,
        header_renderer: Any = None,
        hidden: bool | None = None,
        path: str | None = None,
        renderer: Any = None,
        resizable: bool | None = None,
        row_header: bool | None = None,
        select_all: bool | None = None,
        text_align: str | None = None,
        width: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "auto-select": auto_select,
                "auto-width": auto_width,
                "drag-select": drag_select,
                "flex-grow": flex_grow,
                "footer-part-name": footer_part_name,
                "footer-renderer": footer_renderer,
                "frozen": frozen,
                "frozen-to-end": frozen_to_end,
                "header": header,
                "header-part-name": header_part_name,
                "header-renderer": header_renderer,
                "hidden": hidden,
                "path": path,
                "renderer": renderer,
                "resizable": resizable,
                "row-header": row_header,
                "select-all": select_all,
                "text-align": text_align,
                "width": width,
            },
            **props,
        )


class VaadinGridSortColumn(Component):
    """`<vaadin-grid-sort-column>` is a helper element for the `<vaadin-grid>` that provides default header renderer and functionality for sorting. #### Example: ```html <vaadin-grid> <vaadin-grid-sort-column path="name.first" direction="asc"></vaadin-grid-sort-column> <vaadin-grid-column> ... ```"""

    tag = "vaadin-grid-sort-column"
    schema = ComponentSchema(
        tag="vaadin-grid-sort-column",
        class_name="VaadinGridSortColumn",
        summary='`<vaadin-grid-sort-column>` is a helper element for the `<vaadin-grid>`\nthat provides default header renderer and functionality for sorting.\n\n#### Example:\n```html\n<vaadin-grid>\n <vaadin-grid-sort-column path="name.first" direction="asc"></vaadin-grid-sort-column>\n\n <vaadin-grid-column>\n   ...\n```',
        props=(
            PropertySchema(
                name="auto-width",
                kind="boolean",
                choices=(),
                default=None,
                description="Automatically sets the width of the column based on the column contents when this is set to `true`.\n\nFor performance reasons the column width is calculated automatically only once when the grid items\nare rendered for the first time and the calculation only considers the rows which are currently\nrendered in DOM (a bit more than what is currently visible). If the grid is scrolled, or the cell\ncontent changes, the column width might not match the contents anymore.\n\nHidden columns are ignored in the calculation and their widths are not automatically updated when\nyou show a column that was initially hidden.\n\nYou can manually trigger the auto sizing behavior again by calling `grid.recalculateColumnWidths()`.\n\nThe column width may still grow larger when `flexGrow` is not 0.",
            ),
            PropertySchema(
                name="direction",
                kind="string",
                choices=(),
                default=None,
                description="How to sort the data.\nPossible values are `asc` to use an ascending algorithm, `desc` to sort the data in\ndescending direction, or `null` for not sorting the data.",
            ),
            PropertySchema(
                name="flex-grow",
                kind="number",
                choices=(),
                default=None,
                description="Flex grow ratio for the cell widths. When set to 0, cell width is fixed.",
            ),
            PropertySchema(name="footer-part-name", kind="string", choices=(), default=None, description="Custom part name for the footer cell."),
            PropertySchema(
                name="footer-renderer",
                kind="json",
                choices=(),
                type_text="GridHeaderFooterRenderer | null | undefined",
                default=None,
                description="Custom function for rendering the footer content.\nReceives two arguments:\n\n- `root` The footer cell content DOM element. Append your content to it.\n- `column` The `<vaadin-grid-column>` element.",
            ),
            PropertySchema(
                name="frozen",
                kind="boolean",
                choices=(),
                default=None,
                description="When true, the column is frozen. When a column inside of a column group is frozen,\nall of the sibling columns inside the group will get frozen also.",
            ),
            PropertySchema(
                name="frozen-to-end",
                kind="boolean",
                choices=(),
                default=None,
                description="When true, the column is frozen to end of grid.\n\nWhen a column inside of a column group is frozen to end, all of the sibling columns\ninside the group will get frozen to end also.\n\nColumn can not be set as `frozen` and `frozenToEnd` at the same time.",
            ),
            PropertySchema(
                name="header", kind="string", choices=(), default=None, description="Text content to display in the header cell of the column."
            ),
            PropertySchema(name="header-part-name", kind="string", choices=(), default=None, description="Custom part name for the header cell."),
            PropertySchema(
                name="header-renderer",
                kind="json",
                choices=(),
                type_text="GridHeaderFooterRenderer | null | undefined",
                default=None,
                description="Custom function for rendering the header content.\nReceives two arguments:\n\n- `root` The header cell content DOM element. Append your content to it.\n- `column` The `<vaadin-grid-column>` element.",
            ),
            PropertySchema(
                name="hidden", kind="boolean", choices=(), default=None, description="When set to true, the cells for this column are hidden."
            ),
            PropertySchema(
                name="path", kind="string", choices=(), default=None, description="JS Path of the property in the item used for sorting the data."
            ),
            PropertySchema(
                name="renderer",
                kind="json",
                choices=(),
                type_text="GridBodyRenderer | null | undefined",
                default=None,
                description="Custom function for rendering the cell content.\nReceives three arguments:\n\n- `root` The cell content DOM element. Append your content to it.\n- `column` The `<vaadin-grid-column>` element.\n- `model` The object with the properties related with\n  the rendered item, contains:\n  - `model.index` The index of the item.\n  - `model.item` The item.\n  - `model.expanded` Sublevel toggle state.\n  - `model.level` Level of the tree represented with a horizontal offset of the toggle button.\n  - `model.selected` Selected state.\n  - `model.detailsOpened` Details opened state.\n  - `model.hasChildren` Whether the item has children.",
            ),
            PropertySchema(
                name="resizable", kind="boolean", choices=(), default="false", description="When set to true, the column is user-resizable."
            ),
            PropertySchema(
                name="row-header",
                kind="boolean",
                choices=(),
                default=None,
                description="When true, the cells for this column will be rendered with the `role` attribute\nset as `rowheader`, instead of the `gridcell` role value used by default.\n\nWhen a column is set as row header, its cells will be announced by screen readers\nwhile navigating to help user identify the current row as uniquely as possible.",
            ),
            PropertySchema(
                name="text-align",
                kind="string",
                choices=(),
                default=None,
                description='Aligns the columns cell content horizontally.\nSupported values: "start", "center" and "end".',
            ),
            PropertySchema(
                name="width",
                kind="string",
                choices=(),
                default=None,
                description="Width of the cells for this column.\n\nPlease note that using the `em` length unit is discouraged as\nit might lead to misalignment issues if the header, body, and footer\ncells have different font sizes. Instead, use `rem` if you need\na length unit relative to the font size.",
            ),
        ),
        events=("direction-changed",),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        auto_width: bool | None = None,
        direction: str | None = None,
        flex_grow: float | None = None,
        footer_part_name: str | None = None,
        footer_renderer: Any = None,
        frozen: bool | None = None,
        frozen_to_end: bool | None = None,
        header: str | None = None,
        header_part_name: str | None = None,
        header_renderer: Any = None,
        hidden: bool | None = None,
        path: str | None = None,
        renderer: Any = None,
        resizable: bool | None = None,
        row_header: bool | None = None,
        text_align: str | None = None,
        width: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "auto-width": auto_width,
                "direction": direction,
                "flex-grow": flex_grow,
                "footer-part-name": footer_part_name,
                "footer-renderer": footer_renderer,
                "frozen": frozen,
                "frozen-to-end": frozen_to_end,
                "header": header,
                "header-part-name": header_part_name,
                "header-renderer": header_renderer,
                "hidden": hidden,
                "path": path,
                "renderer": renderer,
                "resizable": resizable,
                "row-header": row_header,
                "text-align": text_align,
                "width": width,
            },
            **props,
        )


class VaadinGridSorter(Component):
    """`<vaadin-grid-sorter>` is a helper element for the `<vaadin-grid>` that provides out-of-the-box UI controls, visual feedback, and handlers for sorting the grid data. #### Example: ```html <vaadin-grid-column id="column"></vaadin-grid-column> ``` ```js const column = document.querySelector('#column'); column.renderer = (root, column, model) => { let sorter = root.firstElementChild; if (!sorter) { sorter = document.createElement('vaadin-grid-sorter'); root.appendChild(sorter); } sorter.path = 'name.first'; }; ``` ### Styling The following shadow DOM parts are available for styling: Part name | Description ----------------|---------------- `content` | The slotted content wrapper `indicators` | The internal sorter indicators. `order` | The internal sorter order The following state attributes are available for styling: Attribute | Description -------------|--------------------------- `direction` | Sort direction of a sorter"""

    tag = "vaadin-grid-sorter"
    schema = ComponentSchema(
        tag="vaadin-grid-sorter",
        class_name="VaadinGridSorter",
        summary="`<vaadin-grid-sorter>` is a helper element for the `<vaadin-grid>` that provides out-of-the-box UI controls,\nvisual feedback, and handlers for sorting the grid data.\n\n#### Example:\n```html\n<vaadin-grid-column id=\"column\"></vaadin-grid-column>\n```\n```js\nconst column = document.querySelector('#column');\ncolumn.renderer = (root, column, model) => {\n  let sorter = root.firstElementChild;\n  if (!sorter) {\n    sorter = document.createElement('vaadin-grid-sorter');\n    root.appendChild(sorter);\n  }\n  sorter.path = 'name.first';\n};\n```\n\n### Styling\n\nThe following shadow DOM parts are available for styling:\n\nPart name | Description\n----------------|----------------\n`content` | The slotted content wrapper\n`indicators` | The internal sorter indicators.\n`order` | The internal sorter order\n\nThe following state attributes are available for styling:\n\nAttribute    | Description\n-------------|---------------------------\n`direction`  | Sort direction of a sorter",
        props=(
            PropertySchema(
                name="direction",
                kind="string",
                choices=(),
                default=None,
                description="How to sort the data.\nPossible values are `asc` to use an ascending algorithm, `desc` to sort the data in\ndescending direction, or `null` for not sorting the data.",
            ),
            PropertySchema(
                name="path", kind="string", choices=(), default=None, description="JS Path of the property in the item used for sorting the data."
            ),
            PropertySchema(
                name="theme",
                kind="string",
                choices=(),
                default=None,
                description="Space-separated theme variants, such as `primary` or `small`, which the element propagates to its parts.",
            ),
        ),
        events=("direction-changed", "sorter-changed"),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        direction: str | None = None,
        path: str | None = None,
        theme: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "direction": direction,
                "path": path,
                "theme": theme,
            },
            **props,
        )


class VaadinGridTreeColumn(Component):
    """`<vaadin-grid-tree-column>` is a helper element for the `<vaadin-grid>` that provides default renderer and functionality for toggling tree/hierarchical items. #### Example: ```html <vaadin-grid> <vaadin-grid-tree-column path="name.first"></vaadin-grid-tree-column> <vaadin-grid-column> ... ```"""

    tag = "vaadin-grid-tree-column"
    schema = ComponentSchema(
        tag="vaadin-grid-tree-column",
        class_name="VaadinGridTreeColumn",
        summary='`<vaadin-grid-tree-column>` is a helper element for the `<vaadin-grid>`\nthat provides default renderer and functionality for toggling tree/hierarchical items.\n\n#### Example:\n```html\n<vaadin-grid>\n <vaadin-grid-tree-column path="name.first"></vaadin-grid-tree-column>\n\n <vaadin-grid-column>\n   ...\n```',
        props=(
            PropertySchema(
                name="auto-width",
                kind="boolean",
                choices=(),
                default=None,
                description="Automatically sets the width of the column based on the column contents when this is set to `true`.\n\nFor performance reasons the column width is calculated automatically only once when the grid items\nare rendered for the first time and the calculation only considers the rows which are currently\nrendered in DOM (a bit more than what is currently visible). If the grid is scrolled, or the cell\ncontent changes, the column width might not match the contents anymore.\n\nHidden columns are ignored in the calculation and their widths are not automatically updated when\nyou show a column that was initially hidden.\n\nYou can manually trigger the auto sizing behavior again by calling `grid.recalculateColumnWidths()`.\n\nThe column width may still grow larger when `flexGrow` is not 0.",
            ),
            PropertySchema(
                name="flex-grow",
                kind="number",
                choices=(),
                default=None,
                description="Flex grow ratio for the cell widths. When set to 0, cell width is fixed.",
            ),
            PropertySchema(name="footer-part-name", kind="string", choices=(), default=None, description="Custom part name for the footer cell."),
            PropertySchema(
                name="footer-renderer",
                kind="json",
                choices=(),
                type_text="GridHeaderFooterRenderer | null | undefined",
                default=None,
                description="Custom function for rendering the footer content.\nReceives two arguments:\n\n- `root` The footer cell content DOM element. Append your content to it.\n- `column` The `<vaadin-grid-column>` element.",
            ),
            PropertySchema(
                name="frozen",
                kind="boolean",
                choices=(),
                default=None,
                description="When true, the column is frozen. When a column inside of a column group is frozen,\nall of the sibling columns inside the group will get frozen also.",
            ),
            PropertySchema(
                name="frozen-to-end",
                kind="boolean",
                choices=(),
                default=None,
                description="When true, the column is frozen to end of grid.\n\nWhen a column inside of a column group is frozen to end, all of the sibling columns\ninside the group will get frozen to end also.\n\nColumn can not be set as `frozen` and `frozenToEnd` at the same time.",
            ),
            PropertySchema(
                name="header", kind="string", choices=(), default=None, description="Text content to display in the header cell of the column."
            ),
            PropertySchema(name="header-part-name", kind="string", choices=(), default=None, description="Custom part name for the header cell."),
            PropertySchema(
                name="header-renderer",
                kind="json",
                choices=(),
                type_text="GridHeaderFooterRenderer | null | undefined",
                default=None,
                description="Custom function for rendering the header content.\nReceives two arguments:\n\n- `root` The header cell content DOM element. Append your content to it.\n- `column` The `<vaadin-grid-column>` element.",
            ),
            PropertySchema(
                name="hidden", kind="boolean", choices=(), default=None, description="When set to true, the cells for this column are hidden."
            ),
            PropertySchema(
                name="path",
                kind="string",
                choices=(),
                default=None,
                description="JS Path of the property in the item used as text content for the tree toggle.",
            ),
            PropertySchema(
                name="renderer",
                kind="json",
                choices=(),
                type_text="GridBodyRenderer | null | undefined",
                default=None,
                description="Custom function for rendering the cell content.\nReceives three arguments:\n\n- `root` The cell content DOM element. Append your content to it.\n- `column` The `<vaadin-grid-column>` element.\n- `model` The object with the properties related with\n  the rendered item, contains:\n  - `model.index` The index of the item.\n  - `model.item` The item.\n  - `model.expanded` Sublevel toggle state.\n  - `model.level` Level of the tree represented with a horizontal offset of the toggle button.\n  - `model.selected` Selected state.\n  - `model.detailsOpened` Details opened state.\n  - `model.hasChildren` Whether the item has children.",
            ),
            PropertySchema(
                name="resizable", kind="boolean", choices=(), default="false", description="When set to true, the column is user-resizable."
            ),
            PropertySchema(
                name="row-header",
                kind="boolean",
                choices=(),
                default=None,
                description="When true, the cells for this column will be rendered with the `role` attribute\nset as `rowheader`, instead of the `gridcell` role value used by default.\n\nWhen a column is set as row header, its cells will be announced by screen readers\nwhile navigating to help user identify the current row as uniquely as possible.",
            ),
            PropertySchema(
                name="text-align",
                kind="string",
                choices=(),
                default=None,
                description='Aligns the columns cell content horizontally.\nSupported values: "start", "center" and "end".',
            ),
            PropertySchema(
                name="width",
                kind="string",
                choices=(),
                default=None,
                description="Width of the cells for this column.\n\nPlease note that using the `em` length unit is discouraged as\nit might lead to misalignment issues if the header, body, and footer\ncells have different font sizes. Instead, use `rem` if you need\na length unit relative to the font size.",
            ),
        ),
        events=(),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        auto_width: bool | None = None,
        flex_grow: float | None = None,
        footer_part_name: str | None = None,
        footer_renderer: Any = None,
        frozen: bool | None = None,
        frozen_to_end: bool | None = None,
        header: str | None = None,
        header_part_name: str | None = None,
        header_renderer: Any = None,
        hidden: bool | None = None,
        path: str | None = None,
        renderer: Any = None,
        resizable: bool | None = None,
        row_header: bool | None = None,
        text_align: str | None = None,
        width: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "auto-width": auto_width,
                "flex-grow": flex_grow,
                "footer-part-name": footer_part_name,
                "footer-renderer": footer_renderer,
                "frozen": frozen,
                "frozen-to-end": frozen_to_end,
                "header": header,
                "header-part-name": header_part_name,
                "header-renderer": header_renderer,
                "hidden": hidden,
                "path": path,
                "renderer": renderer,
                "resizable": resizable,
                "row-header": row_header,
                "text-align": text_align,
                "width": width,
            },
            **props,
        )


class VaadinGridTreeToggle(Component):
    """`<vaadin-grid-tree-toggle>` is a helper element for the `<vaadin-grid>` that provides toggle and level display functionality for the item tree. #### Example: ```html <vaadin-grid-column id="column"></vaadin-grid-column> ``` ```js const column = document.querySelector('#column'); column.renderer = (root, column, model) => { let treeToggle = root.firstElementChild; if (!treeToggle) { treeToggle = document.createElement('vaadin-grid-tree-toggle'); treeToggle.addEventListener('expanded-changed', () => { ... }); root.appendChild(treeToggle); } treeToggle.leaf = !model.item.hasChildren; treeToggle.level = level; treeToggle.expanded = expanded; treeToggle.textContent = model.item.name; }; ``` ### Styling The following shadow DOM parts are available for styling: Part name | Description ---|--- `toggle` | The tree toggle icon The following state attributes are available for styling: Attribute | Description -----------|------------------------------------- `expanded` | When present, the toggle is expanded `leaf` | When present, the toggle is not expandable, i. e., the current item is a leaf The following custom CSS properties are available for styling: Custom CSS property | :--------------------------------------------| | `--vaadin-grid-tree-toggle-level-offset` | See [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation."""

    tag = "vaadin-grid-tree-toggle"
    schema = ComponentSchema(
        tag="vaadin-grid-tree-toggle",
        class_name="VaadinGridTreeToggle",
        summary="`<vaadin-grid-tree-toggle>` is a helper element for the `<vaadin-grid>`\nthat provides toggle and level display functionality for the item tree.\n\n#### Example:\n```html\n<vaadin-grid-column id=\"column\"></vaadin-grid-column>\n```\n```js\nconst column = document.querySelector('#column');\ncolumn.renderer = (root, column, model) => {\n  let treeToggle = root.firstElementChild;\n  if (!treeToggle) {\n    treeToggle = document.createElement('vaadin-grid-tree-toggle');\n    treeToggle.addEventListener('expanded-changed', () => { ... });\n    root.appendChild(treeToggle);\n  }\n  treeToggle.leaf = !model.item.hasChildren;\n  treeToggle.level = level;\n  treeToggle.expanded = expanded;\n  treeToggle.textContent = model.item.name;\n};\n```\n\n### Styling\n\nThe following shadow DOM parts are available for styling:\n\nPart name | Description\n---|---\n`toggle` | The tree toggle icon\n\nThe following state attributes are available for styling:\n\nAttribute  | Description\n-----------|-------------------------------------\n`expanded` | When present, the toggle is expanded\n`leaf`     | When present, the toggle is not expandable, i. e., the current item is a leaf\n\nThe following custom CSS properties are available for styling:\n\nCustom CSS property                          |\n:--------------------------------------------|\n| `--vaadin-grid-tree-toggle-level-offset`   |\n\nSee [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation.",
        props=(
            PropertySchema(name="expanded", kind="boolean", choices=(), default=None, description="Sublevel toggle state."),
            PropertySchema(
                name="leaf", kind="boolean", choices=(), default=None, description="Hides the toggle icon and disables toggling a tree sublevel."
            ),
            PropertySchema(
                name="level",
                kind="number",
                choices=(),
                default=None,
                description="Current level of the tree represented with a horizontal offset\nof the toggle button.",
            ),
            PropertySchema(
                name="theme",
                kind="string",
                choices=(),
                default=None,
                description="Space-separated theme variants, such as `primary` or `small`, which the element propagates to its parts.",
            ),
        ),
        events=("expanded-changed",),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        expanded: bool | None = None,
        leaf: bool | None = None,
        level: float | None = None,
        theme: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "expanded": expanded,
                "leaf": leaf,
                "level": level,
                "theme": theme,
            },
            **props,
        )


class VaadinGrid(Component):
    """`<vaadin-grid>` is a free, high quality data grid / data table Web Component. The content of the the grid can be populated by using renderer callback function. ### Quick Start Start with an assigning an array to the [`items`](#/elements/vaadin-grid#property-items) property to visualize your data. Use the [`<vaadin-grid-column>`](#/elements/vaadin-grid-column) element to configure the grid columns. Set `path` and `header` shorthand properties for the columns to define what gets rendered in the cells of the column. #### Example: ```html <vaadin-grid> <vaadin-grid-column path="name.first" header="First name"></vaadin-grid-column> <vaadin-grid-column path="name.last" header="Last name"></vaadin-grid-column> <vaadin-grid-column path="email"></vaadin-grid-column> </vaadin-grid> ``` For custom content `vaadin-grid-column` element provides you with three types of `renderer` callback functions: `headerRenderer`, `renderer` and `footerRenderer`. Each of those renderer functions provides `root`, `column`, `model` arguments when applicable. Generate DOM content, append it to the `root` element and control the state of the host element by accessing `column`. Before generating new content, users are able to check if there is already content in `root` for reusing it. Renderers are called on initialization of new column cells and each time the related row model is updated. DOM generated during the renderer call can be reused in the next renderer call and will be provided with the `root` argument. On first call it will be empty. #### Example: ```html <vaadin-grid> <vaadin-grid-column></vaadin-grid-column> <vaadin-grid-column></vaadin-grid-column> <vaadin-grid-column></vaadin-grid-column> </vaadin-grid> ``` ```js const grid = document.querySelector('vaadin-grid'); grid.items = [{'name': 'John', 'surname': 'Lennon', 'role': 'singer'}, {'name': 'Ringo', 'surname': 'Starr', 'role': 'drums'}]; const columns = grid.querySelectorAll('vaadin-grid-column'); columns[0].headerRenderer = function(root) { root.textContent = 'Name'; }; columns[0].renderer = function(root, column, model) { root.textContent = model.item.name; }; columns[1].headerRenderer = function(root) { root.textContent = 'Surname'; }; columns[1].renderer = function(root, column, model) { root.textContent = model.item.surname; }; columns[2].headerRenderer = function(root) { root.textContent = 'Role'; }; columns[2].renderer = function(root, column, model) { root.textContent = model.item.role; }; ``` The following properties are available in the `model` argument: Property name | Type | Description --------------|------|------------ `index`| Number | The index of the item. `item` | String or Object | The item. `level` | Number | Number of the item's tree sublevel, starts from 0. `expanded` | Boolean | True if the item's tree sublevel is expanded. `selected` | Boolean | True if the item is selected. `detailsOpened` | Boolean | True if the item's row details are open. `hasChildren` | Boolean | True if the item has children The following helper elements can be used for further customization: - [`<vaadin-grid-column-group>`](#/elements/vaadin-grid-column-group) - [`<vaadin-grid-filter>`](#/elements/vaadin-grid-filter) - [`<vaadin-grid-sorter>`](#/elements/vaadin-grid-sorter) - [`<vaadin-grid-selection-column>`](#/elements/vaadin-grid-selection-column) - [`<vaadin-grid-tree-toggle>`](#/elements/vaadin-grid-tree-toggle) __Note that the helper elements must be explicitly imported.__ If you want to import everything at once you can use the `all-imports.js` entrypoint. ### Lazy Loading with Function Data Provider In addition to assigning an array to the items property, you can alternatively provide the `<vaadin-grid>` data through the [`dataProvider`](#/elements/vaadin-grid#property-dataProvider) function property. The `<vaadin-grid>` calls this function lazily, only when it needs more data to be displayed. See the [`dataProvider`](#/elements/vaadin-grid#property-dataProvider) property documentation for the detailed data provider arguments description. __Note that expanding the tree grid's item will trigger a call to the `dataProvider`.__ __Also, note that when using function data providers, the total number of items needs to be set manually. The total number of items can be returned in the second argument of the data provider callback:__ ```javascript grid.dataProvider = ({page, pageSize}, callback) => { // page: the requested page index // pageSize: number of items on one page const url = `https://api.example/data?page=${page}&per_page=${pageSize}`; fetch(url) .then((res) => res.json()) .then(({ employees, totalSize }) => { callback(employees, totalSize); }); }; ``` __Alternatively, you can use the `size` property to set the total number of items:__ ```javascript grid.size = 200; // The total number of items grid.dataProvider = ({page, pageSize}, callback) => { const url = `https://api.example/data?page=${page}&per_page=${pageSize}`; fetch(url) .then((res) => res.json()) .then((resJson) => callback(resJson.employees)); }; ``` ### Styling The following shadow DOM parts are available for styling: Part name | Description ---------------------------|---------------- `row` | Row in the internal table `body-row` | Body row in the internal table `header-row` | Header row in the internal table `footer-row` | Footer row in the internal table `collapsed-row` | Collapsed row `expanded-row` | Expanded row `selected-row` | Selected row `nonselectable-row` | Row that the user may not select or deselect `details-opened-row` | Row with details open `odd-row` | Odd row `even-row` | Even row `first-row` | The first body row `first-header-row` | The first header row `first-footer-row` | The first footer row `last-row` | The last body row `last-header-row` | The last header row `last-footer-row` | The last footer row `dragstart-row` | Set on the row for one frame when drag is starting. `dragover-above-row` | Set on the row when the a row is dragged over above `dragover-below-row` | Set on the row when the a row is dragged over below `dragover-on-top-row` | Set on the row when the a row is dragged over on top `drag-disabled-row` | Set to a row that isn't available for dragging `drop-disabled-row` | Set to a row that can't be dropped on top of `cell` | Cell in the internal table `header-cell` | Header cell in the internal table `body-cell` | Body cell in the internal table `footer-cell` | Footer cell in the internal table `details-cell` | Row details cell in the internal table `focused-cell` | Focused cell in the internal table `odd-row-cell` | Cell in an odd row `even-row-cell` | Cell in an even row `first-row-cell` | Cell in the first body row `last-row-cell` | Cell in the last body row `first-header-row-cell` | Cell in the first header row `first-footer-row-cell` | Cell in the first footer row `last-header-row-cell` | Cell in the last header row `last-footer-row-cell` | Cell in the last footer row `loading-row-cell` | Cell in a row that is waiting for data from data provider `selected-row-cell` | Cell in a selected row `nonselectable-row-cell` | Cell in a row that the user may not select or deselect `collapsed-row-cell` | Cell in a collapsed row `expanded-row-cell` | Cell in an expanded row `details-opened-row-cell` | Cell in an row with details open `dragstart-row-cell` | Cell in the ghost image row, but not in a source row `drag-source-row-cell` | Cell in a source row, but not in the ghost image `dragover-above-row-cell` | Cell in a row that has another row dragged over above `dragover-below-row-cell` | Cell in a row that has another row dragged over below `dragover-on-top-row-cell` | Cell in a row that has another row dragged over on top `drag-disabled-row-cell` | Cell in a row that isn't available for dragging `drop-disabled-row-cell` | Cell in a row that can't be dropped on top of `frozen-cell` | Frozen cell in the internal table `frozen-to-end-cell` | Frozen to end cell in the internal table `last-frozen-cell` | Last frozen cell `first-frozen-to-end-cell` | First cell frozen to end `first-column-cell` | First visible cell on a row `last-column-cell` | Last visible cell on a row `reorder-allowed-cell` | Cell in a column where another column can be reordered `reorder-dragging-cell` | Cell in a column currently being reordered `resize-handle` | Handle for resizing the columns `empty-state` | The container for the content to be displayed when there are no body rows to show `reorder-ghost` | Ghost element of the header cell being dragged The following state attributes are available for styling: Attribute | Description | Part name ----------------------|---------------------------------------------------------------------------------------------------|----------- `loading` | Set when the grid is loading data from data provider | :host `interacting` | Keyboard navigation in interaction mode | :host `navigating` | Keyboard navigation in navigation mode | :host `overflow` | Set when rows are overflowing the grid viewport. Possible values: `top`, `bottom`, `start`, `end` | :host `reordering` | Set when the grid's columns are being reordered | :host `dragover` | Set when the grid (not a specific row) is dragged over | :host `dragging-rows` | Set when grid rows are dragged | :host `reorder-status` | Reflects the status of a cell while columns are being reordered | cell `frozen` | Frozen cell | cell `frozen-to-end` | Cell frozen to end | cell `last-frozen` | Last frozen cell | cell `first-frozen-to-end` | First cell frozen to end | cell `first-column` | First visible cell on a row | cell `last-column` | Last visible cell on a row | cell `selected` | Selected row | row `expanded` | Expanded row | row `details-opened` | Row with details open | row `loading` | Row that is waiting for data from data provider | row `odd` | Odd row | row `first` | The first body row | row `last` | The last body row | row `dragstart` | Set for one frame when starting to drag a row. The value is a number when dragging multiple rows | row `dragover` | Set when the row is dragged over | row `drag-disabled` | Set to a row that isn't available for dragging | row `drop-disabled` | Set to a row that can't be dropped on top of | row The following custom CSS properties are available for styling: Custom CSS property | :--------------------------------------------------| | `--vaadin-grid-background` | | `--vaadin-grid-border-color` | | `--vaadin-grid-border-radius` | | `--vaadin-grid-border-width` | | `--vaadin-grid-cell-background-color` | | `--vaadin-grid-cell-padding` | | `--vaadin-grid-cell-text-overflow` | | `--vaadin-grid-column-border-width` | | `--vaadin-grid-column-resize-handle-color` | | `--vaadin-grid-header-font-size` | | `--vaadin-grid-header-font-weight` | | `--vaadin-grid-header-text-color` | | `--vaadin-grid-row-background-color` | | `--vaadin-grid-row-border-width` | | `--vaadin-grid-row-highlight-background-color` | | `--vaadin-grid-row-hover-background-color` | | `--vaadin-grid-row-odd-background-color` | | `--vaadin-grid-row-selected-background-color` | See [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation."""

    tag = "vaadin-grid"
    schema = ComponentSchema(
        tag="vaadin-grid",
        class_name="VaadinGrid",
        summary="`<vaadin-grid>` is a free, high quality data grid / data table Web Component. The content of the\nthe grid can be populated by using renderer callback function.\n\n### Quick Start\n\nStart with an assigning an array to the [`items`](#/elements/vaadin-grid#property-items) property to visualize your data.\n\nUse the [`<vaadin-grid-column>`](#/elements/vaadin-grid-column) element to configure the grid columns. Set `path` and `header`\nshorthand properties for the columns to define what gets rendered in the cells of the column.\n\n#### Example:\n```html\n<vaadin-grid>\n  <vaadin-grid-column path=\"name.first\" header=\"First name\"></vaadin-grid-column>\n  <vaadin-grid-column path=\"name.last\" header=\"Last name\"></vaadin-grid-column>\n  <vaadin-grid-column path=\"email\"></vaadin-grid-column>\n</vaadin-grid>\n```\n\nFor custom content `vaadin-grid-column` element provides you with three types of `renderer` callback functions: `headerRenderer`,\n`renderer` and `footerRenderer`.\n\nEach of those renderer functions provides `root`, `column`, `model` arguments when applicable.\nGenerate DOM content, append it to the `root` element and control the state\nof the host element by accessing `column`. Before generating new content,\nusers are able to check if there is already content in `root` for reusing it.\n\nRenderers are called on initialization of new column cells and each time the\nrelated row model is updated. DOM generated during the renderer call can be reused\nin the next renderer call and will be provided with the `root` argument.\nOn first call it will be empty.\n\n#### Example:\n```html\n<vaadin-grid>\n  <vaadin-grid-column></vaadin-grid-column>\n  <vaadin-grid-column></vaadin-grid-column>\n  <vaadin-grid-column></vaadin-grid-column>\n</vaadin-grid>\n```\n```js\nconst grid = document.querySelector('vaadin-grid');\ngrid.items = [{'name': 'John', 'surname': 'Lennon', 'role': 'singer'},\n              {'name': 'Ringo', 'surname': 'Starr', 'role': 'drums'}];\n\nconst columns = grid.querySelectorAll('vaadin-grid-column');\n\ncolumns[0].headerRenderer = function(root) {\n  root.textContent = 'Name';\n};\ncolumns[0].renderer = function(root, column, model) {\n  root.textContent = model.item.name;\n};\n\ncolumns[1].headerRenderer = function(root) {\n  root.textContent = 'Surname';\n};\ncolumns[1].renderer = function(root, column, model) {\n  root.textContent = model.item.surname;\n};\n\ncolumns[2].headerRenderer = function(root) {\n  root.textContent = 'Role';\n};\ncolumns[2].renderer = function(root, column, model) {\n  root.textContent = model.item.role;\n};\n```\n\nThe following properties are available in the `model` argument:\n\nProperty name | Type | Description\n--------------|------|------------\n`index`| Number | The index of the item.\n`item` | String or Object | The item.\n`level` | Number | Number of the item's tree sublevel, starts from 0.\n`expanded` | Boolean | True if the item's tree sublevel is expanded.\n`selected` | Boolean | True if the item is selected.\n`detailsOpened` | Boolean | True if the item's row details are open.\n`hasChildren` | Boolean | True if the item has children\n\nThe following helper elements can be used for further customization:\n- [`<vaadin-grid-column-group>`](#/elements/vaadin-grid-column-group)\n- [`<vaadin-grid-filter>`](#/elements/vaadin-grid-filter)\n- [`<vaadin-grid-sorter>`](#/elements/vaadin-grid-sorter)\n- [`<vaadin-grid-selection-column>`](#/elements/vaadin-grid-selection-column)\n- [`<vaadin-grid-tree-toggle>`](#/elements/vaadin-grid-tree-toggle)\n\n__Note that the helper elements must be explicitly imported.__\nIf you want to import everything at once you can use the `all-imports.js` entrypoint.\n\n### Lazy Loading with Function Data Provider\n\nIn addition to assigning an array to the items property, you can alternatively\nprovide the `<vaadin-grid>` data through the\n[`dataProvider`](#/elements/vaadin-grid#property-dataProvider) function property.\nThe `<vaadin-grid>` calls this function lazily, only when it needs more data\nto be displayed.\n\nSee the [`dataProvider`](#/elements/vaadin-grid#property-dataProvider) property\ndocumentation for the detailed data provider arguments description.\n\n__Note that expanding the tree grid's item will trigger a call to the `dataProvider`.__\n\n__Also, note that when using function data providers, the total number of items\nneeds to be set manually. The total number of items can be returned\nin the second argument of the data provider callback:__\n\n```javascript\ngrid.dataProvider = ({page, pageSize}, callback) => {\n  // page: the requested page index\n  // pageSize: number of items on one page\n  const url = `https://api.example/data?page=${page}&per_page=${pageSize}`;\n\n  fetch(url)\n    .then((res) => res.json())\n    .then(({ employees, totalSize }) => {\n      callback(employees, totalSize);\n    });\n};\n```\n\n__Alternatively, you can use the `size` property to set the total number of items:__\n\n```javascript\ngrid.size = 200; // The total number of items\ngrid.dataProvider = ({page, pageSize}, callback) => {\n  const url = `https://api.example/data?page=${page}&per_page=${pageSize}`;\n\n  fetch(url)\n    .then((res) => res.json())\n    .then((resJson) => callback(resJson.employees));\n};\n```\n\n### Styling\n\nThe following shadow DOM parts are available for styling:\n\nPart name                  | Description\n---------------------------|----------------\n`row`                      | Row in the internal table\n`body-row`                 | Body row in the internal table\n`header-row`               | Header row in the internal table\n`footer-row`               | Footer row in the internal table\n`collapsed-row`            | Collapsed row\n`expanded-row`             | Expanded row\n`selected-row`             | Selected row\n`nonselectable-row`        | Row that the user may not select or deselect\n`details-opened-row`       | Row with details open\n`odd-row`                  | Odd row\n`even-row`                 | Even row\n`first-row`                | The first body row\n`first-header-row`         | The first header row\n`first-footer-row`         | The first footer row\n`last-row`                 | The last body row\n`last-header-row`          | The last header row\n`last-footer-row`          | The last footer row\n`dragstart-row`            | Set on the row for one frame when drag is starting.\n`dragover-above-row`       | Set on the row when the a row is dragged over above\n`dragover-below-row`       | Set on the row when the a row is dragged over below\n`dragover-on-top-row`      | Set on the row when the a row is dragged over on top\n`drag-disabled-row`        | Set to a row that isn't available for dragging\n`drop-disabled-row`        | Set to a row that can't be dropped on top of\n`cell`                     | Cell in the internal table\n`header-cell`              | Header cell in the internal table\n`body-cell`                | Body cell in the internal table\n`footer-cell`              | Footer cell in the internal table\n`details-cell`             | Row details cell in the internal table\n`focused-cell`             | Focused cell in the internal table\n`odd-row-cell`             | Cell in an odd row\n`even-row-cell`            | Cell in an even row\n`first-row-cell`           | Cell in the first body row\n`last-row-cell`            | Cell in the last body row\n`first-header-row-cell`    | Cell in the first header row\n`first-footer-row-cell`    | Cell in the first footer row\n`last-header-row-cell`     | Cell in the last header row\n`last-footer-row-cell`     | Cell in the last footer row\n`loading-row-cell`         | Cell in a row that is waiting for data from data provider\n`selected-row-cell`        | Cell in a selected row\n`nonselectable-row-cell`   | Cell in a row that the user may not select or deselect\n`collapsed-row-cell`       | Cell in a collapsed row\n`expanded-row-cell`        | Cell in an expanded row\n`details-opened-row-cell`  | Cell in an row with details open\n`dragstart-row-cell`       | Cell in the ghost image row, but not in a source row\n`drag-source-row-cell`     | Cell in a source row, but not in the ghost image\n`dragover-above-row-cell`  | Cell in a row that has another row dragged over above\n`dragover-below-row-cell`  | Cell in a row that has another row dragged over below\n`dragover-on-top-row-cell` | Cell in a row that has another row dragged over on top\n`drag-disabled-row-cell`   | Cell in a row that isn't available for dragging\n`drop-disabled-row-cell`   | Cell in a row that can't be dropped on top of\n`frozen-cell`              | Frozen cell in the internal table\n`frozen-to-end-cell`       | Frozen to end cell in the internal table\n`last-frozen-cell`         | Last frozen cell\n`first-frozen-to-end-cell` | First cell frozen to end\n`first-column-cell`        | First visible cell on a row\n`last-column-cell`         | Last visible cell on a row\n`reorder-allowed-cell`     | Cell in a column where another column can be reordered\n`reorder-dragging-cell`    | Cell in a column currently being reordered\n`resize-handle`            | Handle for resizing the columns\n`empty-state`              | The container for the content to be displayed when there are no body rows to show\n`reorder-ghost`            | Ghost element of the header cell being dragged\n\nThe following state attributes are available for styling:\n\nAttribute             | Description                                                                                       | Part name\n----------------------|---------------------------------------------------------------------------------------------------|-----------\n`loading`             | Set when the grid is loading data from data provider                                              | :host\n`interacting`         | Keyboard navigation in interaction mode                                                           | :host\n`navigating`          | Keyboard navigation in navigation mode                                                            | :host\n`overflow`            | Set when rows are overflowing the grid viewport. Possible values: `top`, `bottom`, `start`, `end` | :host\n`reordering`          | Set when the grid's columns are being reordered                                                   | :host\n`dragover`            | Set when the grid (not a specific row) is dragged over                                            | :host\n`dragging-rows`       | Set when grid rows are dragged                                                                    | :host\n`reorder-status`      | Reflects the status of a cell while columns are being reordered                                   | cell\n`frozen`              | Frozen cell                                                                                       | cell\n`frozen-to-end`       | Cell frozen to end                                                                                | cell\n`last-frozen`         | Last frozen cell                                                                                  | cell\n`first-frozen-to-end` | First cell frozen to end                                                                          | cell\n`first-column`        | First visible cell on a row                                                                       | cell\n`last-column`         | Last visible cell on a row                                                                        | cell\n`selected`            | Selected row                                                                                      | row\n`expanded`            | Expanded row                                                                                      | row\n`details-opened`      | Row with details open                                                                             | row\n`loading`             | Row that is waiting for data from data provider                                                   | row\n`odd`                 | Odd row                                                                                           | row\n`first`               | The first body row                                                                                | row\n`last`                | The last body row                                                                                 | row\n`dragstart`           | Set for one frame when starting to drag a row. The value is a number when dragging multiple rows  | row\n`dragover`            | Set when the row is dragged over                                                                  | row\n`drag-disabled`       | Set to a row that isn't available for dragging                                                    | row\n`drop-disabled`       | Set to a row that can't be dropped on top of                                                      | row\n\nThe following custom CSS properties are available for styling:\n\nCustom CSS property                                |\n:--------------------------------------------------|\n| `--vaadin-grid-background`                       |\n| `--vaadin-grid-border-color`                     |\n| `--vaadin-grid-border-radius`                    |\n| `--vaadin-grid-border-width`                     |\n| `--vaadin-grid-cell-background-color`            |\n| `--vaadin-grid-cell-padding`                     |\n| `--vaadin-grid-cell-text-overflow`               |\n| `--vaadin-grid-column-border-width`              |\n| `--vaadin-grid-column-resize-handle-color`       |\n| `--vaadin-grid-header-font-size`                 |\n| `--vaadin-grid-header-font-weight`               |\n| `--vaadin-grid-header-text-color`                |\n| `--vaadin-grid-row-background-color`             |\n| `--vaadin-grid-row-border-width`                 |\n| `--vaadin-grid-row-highlight-background-color`   |\n| `--vaadin-grid-row-hover-background-color`       |\n| `--vaadin-grid-row-odd-background-color`         |\n| `--vaadin-grid-row-selected-background-color`    |\n\nSee [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation.",
        props=(
            PropertySchema(
                name="accessible-name", kind="string", choices=(), default=None, description="String used to label the grid to screen reader users."
            ),
            PropertySchema(
                name="active-item",
                kind="json",
                choices=(),
                type_text="GridItem",
                default=None,
                description="The item user has last interacted with. Turns to `null` after user deactivates\nthe item by re-interacting with the currently active item.",
            ),
            PropertySchema(
                name="all-rows-visible",
                kind="boolean",
                choices=(),
                default=None,
                description="If true, the grid's height is defined by its rows.\n\nEffectively, this disables the grid's virtual scrolling so that all the rows are rendered in the DOM at once.\nIf the grid has a large number of items, using the feature is discouraged to avoid performance issues.",
            ),
            PropertySchema(
                name="cell-part-name-generator",
                kind="json",
                choices=(),
                type_text="GridCellPartNameGenerator | null | undefined",
                default=None,
                description="A function that allows generating CSS `part` names for grid cells in Shadow DOM based\non their row and column, for styling from outside using the `::part()` selector.\n\nThe return value should be the generated part name as a string, or multiple part names\nseparated by whitespace characters.\n\nReceives two arguments:\n- `column` The `<vaadin-grid-column>` element (`undefined` for details-cell).\n- `model` The object with the properties related with\n  the rendered item, contains:\n  - `model.index` The index of the item.\n  - `model.item` The item.\n  - `model.expanded` Sublevel toggle state.\n  - `model.level` Level of the tree represented with a horizontal offset of the toggle button.\n  - `model.selected` Selected state.",
            ),
            PropertySchema(
                name="column-rendering",
                kind="string",
                choices=(),
                default=None,
                description='Allows you to choose between modes for rendering columns in the grid:\n\n"eager" (default): All columns are rendered upfront, regardless of their visibility within the viewport.\nThis mode should generally be preferred, as it avoids the limitations imposed by the "lazy" mode.\nUse this mode unless the grid has a large number of columns and performance outweighs the limitations\nin priority.\n\n"lazy": Optimizes the rendering of cells when there are multiple columns in the grid by virtualizing\nhorizontal scrolling. In this mode, body cells are rendered only when their corresponding columns are\ninside the visible viewport.\n\nUsing "lazy" rendering should be used only if you\'re dealing with a large number of columns and performance\nis your highest priority. For most use cases, the default "eager" mode is recommended due to the\nlimitations imposed by the "lazy" mode.\n\nWhen using the "lazy" mode, keep the following limitations in mind:\n\n- Row Height: When only a number of columns are visible at once, the height of a row can only be that of\nthe highest cell currently visible on that row. Make sure each cell on a single row has the same height\nas all other cells on that row. If row cells have different heights, users may experience jumpiness when\nscrolling the grid horizontally as lazily rendered cells with different heights are scrolled into view.\n\n- Auto-width Columns: For the columns that are initially outside the visible viewport but still use auto-width,\nonly the header content is taken into account when calculating the column width because the body cells\nof the columns outside the viewport are not initially rendered.\n\n- Screen Reader Compatibility: Screen readers may not be able to associate the focused cells with the correct\nheaders when only a subset of the body cells on a row is rendered.\n\n- Keyboard Navigation: Tabbing through focusable elements inside the grid body may not work as expected because\nsome of the columns that would include focusable elements in the body cells may be outside the visible viewport\nand thus not rendered.',
            ),
            PropertySchema(
                name="column-reordering-allowed", kind="boolean", choices=(), default=None, description="Set to true to allow column reordering."
            ),
            PropertySchema(
                name="data-provider",
                kind="json",
                choices=(),
                type_text="GridDataProvider | null | undefined",
                default=None,
                description="Function that provides items lazily. Receives arguments `params`, `callback`\n\n`params.page` Requested page index\n\n`params.pageSize` Current page size\n\n`params.filters` Currently applied filters\n\n`params.sortOrders` Currently applied sorting orders\n\n`params.parentItem` When tree is used, and sublevel items\nare requested, reference to parent item of the requested sublevel.\nOtherwise `undefined`.\n\n`callback(items, size)` Callback function with arguments:\n  - `items` Current page of items\n  - `size` Total number of items. When tree sublevel items\n    are requested, total number of items in the requested sublevel.\n    Optional when tree is not used, required for tree.",
            ),
            PropertySchema(
                name="disabled", kind="boolean", choices=(), default=None, description="If true, the user cannot interact with this element."
            ),
            PropertySchema(
                name="drag-filter",
                kind="json",
                choices=(),
                type_text="GridDragAndDropFilter | null | undefined",
                default=None,
                description="A function that filters dragging of specific grid rows. The return value should be false\nif dragging of the row should be disabled.\n\nReceives one argument:\n- `model` The object with the properties related with\n  the rendered item, contains:\n  - `model.index` The index of the item.\n  - `model.item` The item.\n  - `model.expanded` Sublevel toggle state.\n  - `model.level` Level of the tree represented with a horizontal offset of the toggle button.\n  - `model.selected` Selected state.",
            ),
            PropertySchema(
                name="drop-filter",
                kind="json",
                choices=(),
                type_text="GridDragAndDropFilter | null | undefined",
                default=None,
                description="A function that filters dropping on specific grid rows. The return value should be false\nif dropping on the row should be disabled.\n\nReceives one argument:\n- `model` The object with the properties related with\n  the rendered item, contains:\n  - `model.index` The index of the item.\n  - `model.item` The item.\n  - `model.expanded` Sublevel toggle state.\n  - `model.level` Level of the tree represented with a horizontal offset of the toggle button.\n  - `model.selected` Selected state.",
            ),
            PropertySchema(
                name="drop-mode",
                kind="string",
                choices=(),
                default=None,
                description="Defines the locations within the Grid row where an element can be dropped.\n\nPossible values are:\n- `between`: The drop event can happen between Grid rows.\n- `on-top`: The drop event can happen on top of Grid rows.\n- `on-top-or-between`: The drop event can happen either on top of or between Grid rows.\n- `on-grid`: The drop event will not happen on any specific row, it will show the drop target outline around the whole grid.",
            ),
            PropertySchema(
                name="is-item-selectable",
                kind="json",
                choices=(),
                type_text="(item: GridItem) => boolean",
                default=None,
                description="A function to check whether a specific item in the grid may be\nselected or deselected by the user. Used by the selection column to\nconditionally enable to disable checkboxes for individual items. This\nfunction does not prevent programmatic selection/deselection of\nitems. Changing the function does not modify the currently selected\nitems.\n\nConfiguring this function hides the select all checkbox of the grid\nselection column, which means users can not select or deselect all\nitems anymore, nor do they get feedback on whether all items are\nselected or not.\n\nReceives an item instance and should return a boolean indicating\nwhether users may change the selection state of that item.",
            ),
            PropertySchema(
                name="item-has-children-path",
                kind="string",
                choices=(),
                default=None,
                description="Path to an item sub-property that indicates whether the item has child items.",
            ),
            PropertySchema(
                name="item-id-path", kind="string", choices=(), default=None, description="Path to an item sub-property that identifies the item."
            ),
            PropertySchema(
                name="loading", kind="boolean", choices=(), default=None, description="`true` while data is being requested from the data provider."
            ),
            PropertySchema(
                name="multi-sort",
                kind="boolean",
                choices=(),
                default=None,
                description="When `true`, all `<vaadin-grid-sorter>` are applied for sorting.",
            ),
            PropertySchema(
                name="multi-sort-on-shift-click",
                kind="boolean",
                choices=(),
                default=None,
                description="When `true`, Shift-clicking an unsorted column's sorter adds it to the multi-sort.\nShift + Space does the same action via keyboard. This property has precedence over the\n`multiSort` property. If `multiSortOnShiftClick` is true, the multiSort property is effectively ignored.",
            ),
            PropertySchema(
                name="multi-sort-priority",
                kind="string",
                choices=(),
                default=None,
                description='Controls how columns are added to the sort order when using multi-sort.\nThe sort order is visually indicated by numbers in grid sorters placed in column headers.\n\nBy default, whenever an unsorted column is sorted, or the sort-direction of a column is\nchanged, that column gets sort priority 1, thus affecting the priority for all the other\nsorted columns. This is identical to using `multi-sort-priority="prepend"`.\n\nUsing this property allows to change this behavior so that sorting an unsorted column\nwould add it to the "end" of the sort, and changing column\'s sort direction would retain\nit\'s previous priority. To set this, use `multi-sort-priority="append"`.',
            ),
            PropertySchema(
                name="page-size", kind="number", choices=(), default=None, description="Number of items fetched at a time from the dataprovider."
            ),
            PropertySchema(
                name="row-details-renderer",
                kind="json",
                choices=(),
                type_text="GridRowDetailsRenderer | null | undefined",
                default=None,
                description="Custom function for rendering the content of the row details.\nReceives three arguments:\n\n- `root` The row details content DOM element. Append your content to it.\n- `grid` The `<vaadin-grid>` element.\n- `model` The object with the properties related with\n  the rendered item, contains:\n  - `model.index` The index of the item.\n  - `model.item` The item.\n  - `model.level` The number of the item's tree sublevel, starts from 0.\n  - `model.expanded` True if the item's tree sublevel is expanded.\n  - `model.selected` True if the item is selected.",
            ),
            PropertySchema(
                name="rows-draggable", kind="boolean", choices=(), default=None, description="Marks the grid's rows to be available for dragging."
            ),
            PropertySchema(name="size", kind="number", choices=(), default=None, description="The number of root-level items in the grid."),
            PropertySchema(
                name="theme",
                kind="string",
                choices=(),
                default=None,
                description="Space-separated theme variants, such as `primary` or `small`, which the element propagates to its parts.",
            ),
        ),
        fields=(
            PropertySchema(
                name="detailsOpenedItems",
                kind="json",
                choices=(),
                type_text="Array<GridItem>",
                default=None,
                description="An array containing references to items with open row details.",
            ),
            PropertySchema(
                name="expandedItems",
                kind="json",
                choices=(),
                type_text="Array<GridItem>",
                default=None,
                description="An array that contains the expanded items.",
            ),
            PropertySchema(
                name="items",
                kind="json",
                choices=(),
                type_text="Array<GridItem> | undefined",
                default=None,
                description="An array containing the items which will be passed to renderer functions.",
            ),
            PropertySchema(
                name="selectedItems",
                kind="json",
                choices=(),
                type_text="Array<GridItem>",
                default=None,
                description="An array that contains the selected items.",
            ),
        ),
        events=(
            "active-item-changed",
            "cell-activate",
            "cell-focus",
            "column-reorder",
            "column-resize",
            "data-provider-changed",
            "expanded-items-changed",
            "grid-dragend",
            "grid-dragstart",
            "grid-drop",
            "item-toggle",
            "loading-changed",
            "selected-items-changed",
            "size-changed",
        ),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        accessible_name: str | None = None,
        active_item: Any = None,
        all_rows_visible: bool | None = None,
        cell_part_name_generator: Any = None,
        column_rendering: str | None = None,
        column_reordering_allowed: bool | None = None,
        data_provider: Any = None,
        disabled: bool | None = None,
        drag_filter: Any = None,
        drop_filter: Any = None,
        drop_mode: str | None = None,
        is_item_selectable: Any = None,
        item_has_children_path: str | None = None,
        item_id_path: str | None = None,
        loading: bool | None = None,
        multi_sort: bool | None = None,
        multi_sort_on_shift_click: bool | None = None,
        multi_sort_priority: str | None = None,
        page_size: float | None = None,
        row_details_renderer: Any = None,
        rows_draggable: bool | None = None,
        size: float | None = None,
        theme: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "accessible-name": accessible_name,
                "active-item": active_item,
                "all-rows-visible": all_rows_visible,
                "cell-part-name-generator": cell_part_name_generator,
                "column-rendering": column_rendering,
                "column-reordering-allowed": column_reordering_allowed,
                "data-provider": data_provider,
                "disabled": disabled,
                "drag-filter": drag_filter,
                "drop-filter": drop_filter,
                "drop-mode": drop_mode,
                "is-item-selectable": is_item_selectable,
                "item-has-children-path": item_has_children_path,
                "item-id-path": item_id_path,
                "loading": loading,
                "multi-sort": multi_sort,
                "multi-sort-on-shift-click": multi_sort_on_shift_click,
                "multi-sort-priority": multi_sort_priority,
                "page-size": page_size,
                "row-details-renderer": row_details_renderer,
                "rows-draggable": rows_draggable,
                "size": size,
                "theme": theme,
            },
            **props,
        )


class VaadinNotification(Component):
    """`<vaadin-notification>` is a Web Component providing accessible and customizable notifications (toasts). ### Rendering The content of the notification can be populated by using the renderer callback function. The renderer function provides `root`, `notification` arguments. Generate DOM content, append it to the `root` element and control the state of the host element by accessing `notification`. Before generating new content, users are able to check if there is already content in `root` for reusing it. ```html <vaadin-notification id="notification"></vaadin-notification> ``` ```js const notification = document.querySelector('#notification'); notification.renderer = function(root, notification) { root.textContent = "Your work has been saved"; }; ``` Renderer is called on the opening of the notification. DOM generated during the renderer call can be reused in the next renderer call and will be provided with the `root` argument. On first call it will be empty. ### Styling `<vaadin-notification>` uses `<vaadin-notification-card>` internal themable component as the actual visible notification cards. The following shadow DOM parts of the `<vaadin-notification-card>` are available for styling: Part name | Description ----------|---------------- `overlay` | The notification container `content` | The content of the notification The following custom CSS properties are available for styling: Custom CSS property | :-----------------------------------------| | `--vaadin-notification-background` | | `--vaadin-notification-border-color` | | `--vaadin-notification-border-radius` | | `--vaadin-notification-border-width` | | `--vaadin-notification-container-gap` | | `--vaadin-notification-padding` | | `--vaadin-notification-shadow` | | `--vaadin-notification-viewport-inset` | | `--vaadin-notification-width` | See [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation. Note: the `theme` attribute value set on `<vaadin-notification>` is propagated to the internal `<vaadin-notification-card>`."""

    tag = "vaadin-notification"
    schema = ComponentSchema(
        tag="vaadin-notification",
        class_name="VaadinNotification",
        summary='`<vaadin-notification>` is a Web Component providing accessible and customizable notifications (toasts).\n\n### Rendering\n\nThe content of the notification can be populated by using the renderer callback function.\n\nThe renderer function provides `root`, `notification` arguments.\nGenerate DOM content, append it to the `root` element and control the state\nof the host element by accessing `notification`. Before generating new content,\nusers are able to check if there is already content in `root` for reusing it.\n\n```html\n<vaadin-notification id="notification"></vaadin-notification>\n```\n```js\nconst notification = document.querySelector(\'#notification\');\nnotification.renderer = function(root, notification) {\n  root.textContent = "Your work has been saved";\n};\n```\n\nRenderer is called on the opening of the notification.\nDOM generated during the renderer call can be reused\nin the next renderer call and will be provided with the `root` argument.\nOn first call it will be empty.\n\n### Styling\n\n`<vaadin-notification>` uses `<vaadin-notification-card>` internal\nthemable component as the actual visible notification cards.\n\nThe following shadow DOM parts of the `<vaadin-notification-card>` are available for styling:\n\nPart name | Description\n----------|----------------\n`overlay` | The notification container\n`content` | The content of the notification\n\nThe following custom CSS properties are available for styling:\n\nCustom CSS property                       |\n:-----------------------------------------|\n| `--vaadin-notification-background`      |\n| `--vaadin-notification-border-color`    |\n| `--vaadin-notification-border-radius`   |\n| `--vaadin-notification-border-width`    |\n| `--vaadin-notification-container-gap`   |\n| `--vaadin-notification-padding`         |\n| `--vaadin-notification-shadow`          |\n| `--vaadin-notification-viewport-inset`  |\n| `--vaadin-notification-width`           |\n\nSee [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation.\n\nNote: the `theme` attribute value set on `<vaadin-notification>` is\npropagated to the internal `<vaadin-notification-card>`.',
        props=(
            PropertySchema(
                name="assertive",
                kind="boolean",
                choices=(),
                default=None,
                description="When true, the notification card has `aria-live` attribute set to\n`assertive` instead of `polite`. This makes screen readers announce\nthe notification content immediately when it appears.",
            ),
            PropertySchema(
                name="duration",
                kind="number",
                choices=(),
                default=None,
                description="The duration in milliseconds to show the notification.\nSet to `0` or a negative number to disable the notification auto-closing.",
            ),
            PropertySchema(name="opened", kind="boolean", choices=(), default=None, description="True if the notification is currently displayed."),
            PropertySchema(
                name="overlay-class",
                kind="string",
                choices=(),
                default=None,
                description="A space-delimited list of CSS class names to set on the overlay element.\nThis property does not affect other CSS class names set manually via JS.\n\nNote, if the CSS class name was set with this property, clearing it will\nremove it from the overlay, even if the same class name was also added\nmanually, e.g. by using `classList.add()` in the `renderer` function.",
            ),
            PropertySchema(
                name="position",
                kind="string",
                choices=(),
                default=None,
                description="Alignment of the notification in the viewport\nValid values are `top-stretch|top-start|top-center|top-end|middle|bottom-start|bottom-center|bottom-end|bottom-stretch`",
            ),
            PropertySchema(
                name="renderer",
                kind="json",
                choices=(),
                type_text="NotificationRenderer | undefined",
                default=None,
                description="Custom function for rendering the content of the notification.\nReceives two arguments:\n\n- `root` The `<vaadin-notification-card>` DOM element. Append\n  your content to it.\n- `notification` The reference to the `<vaadin-notification>` element.",
            ),
            PropertySchema(
                name="theme",
                kind="string",
                choices=(),
                default=None,
                description="Space-separated theme variants, such as `primary` or `small`, which the element propagates to its parts.",
            ),
        ),
        events=("closed", "opened-changed"),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        assertive: bool | None = None,
        duration: float | None = None,
        opened: bool | None = None,
        overlay_class: str | None = None,
        position: str | None = None,
        renderer: Any = None,
        theme: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "assertive": assertive,
                "duration": duration,
                "opened": opened,
                "overlay-class": overlay_class,
                "position": position,
                "renderer": renderer,
                "theme": theme,
            },
            **props,
        )


class VaadinSelect(Component):
    """`<vaadin-select>` is a Web Component for selecting values from a list of items. ### Items Use the `items` property to define possible options for the select: ```html <vaadin-select id="select"></vaadin-select> ``` ```js const select = document.querySelector('#select'); select.items = [ { label: 'Most recent first', value: 'recent' }, { component: 'hr' }, { label: 'Rating: low to high', value: 'rating-asc', className: 'asc' }, { label: 'Rating: high to low', value: 'rating-desc', className: 'desc' }, { component: 'hr' }, { label: 'Price: low to high', value: 'price-asc', disabled: true }, { label: 'Price: high to low', value: 'price-desc', disabled: true } ]; ``` ### Rendering Alternatively, the content of the select can be populated by using the renderer callback function. The renderer function provides `root`, `select` arguments. Generate DOM content, append it to the `root` element and control the state of the host element by accessing `select`. ```js const select = document.querySelector('#select'); select.renderer = function(root, select) { const listBox = document.createElement('vaadin-list-box'); // append 3 <vaadin-item> elements ['Jose', 'Manolo', 'Pedro'].forEach(function(name) { const item = document.createElement('vaadin-item'); item.textContent = name; item.setAttribute('label', name) listBox.appendChild(item); }); // update the content root.appendChild(listBox); }; ``` Renderer is called on initialization of new select and on its opening. DOM generated during the renderer call can be reused in the next renderer call and will be provided with the `root` argument. On first call it will be empty. * Hint: By setting the `label` property of inner vaadin-items you will be able to change the visual representation of the selected value in the input part. ### Styling The following shadow DOM parts are available for styling: Part name | Description ---------------------|---------------- `label` | The label element `input-field` | The element that wraps prefix, value and toggle button `field-button` | Set on the toggle button `error-message` | The error message element `helper-text` | The helper text element wrapper `required-indicator` | The `required` state indicator element `toggle-button` | The toggle button `backdrop` | Backdrop of the overlay `overlay` | The overlay container `content` | The overlay content The following state attributes are available for styling: Attribute | Description ---------------------|--------------------------------- `disabled` | Set when the element is disabled `has-value` | Set when the element has a value `has-label` | Set when the element has a label `has-helper` | Set when the element has helper text or slot `has-error-message` | Set when the element has an error message `invalid` | Set when the element is invalid `focused` | Set when the element is focused `focus-ring` | Set when the element is keyboard focused `readonly` | Set when the element is readonly `opened` | Set when the overlay is opened `phone` | Set when the overlay is shown in phone mode The following custom CSS properties are available for styling: Custom CSS property | :--------------------------------------------------| | `--vaadin-field-default-width` | | `--vaadin-input-field-background` | | `--vaadin-input-field-border-color` | | `--vaadin-input-field-border-radius` | | `--vaadin-input-field-border-width` | | `--vaadin-input-field-bottom-end-radius` | | `--vaadin-input-field-bottom-start-radius` | | `--vaadin-input-field-button-text-color` | | `--vaadin-input-field-container-gap` | | `--vaadin-input-field-disabled-background` | | `--vaadin-input-field-disabled-text-color` | | `--vaadin-input-field-error-color` | | `--vaadin-input-field-error-font-size` | | `--vaadin-input-field-error-font-weight` | | `--vaadin-input-field-error-line-height` | | `--vaadin-input-field-gap` | | `--vaadin-input-field-helper-color` | | `--vaadin-input-field-helper-font-size` | | `--vaadin-input-field-helper-font-weight` | | `--vaadin-input-field-helper-line-height` | | `--vaadin-input-field-label-color` | | `--vaadin-input-field-label-font-size` | | `--vaadin-input-field-label-font-weight` | | `--vaadin-input-field-label-line-height` | | `--vaadin-input-field-padding` | | `--vaadin-input-field-placeholder-color` | | `--vaadin-input-field-required-indicator` | | `--vaadin-input-field-required-indicator-color` | | `--vaadin-input-field-top-end-radius` | | `--vaadin-input-field-top-start-radius` | | `--vaadin-input-field-value-color` | | `--vaadin-input-field-value-font-size` | | `--vaadin-input-field-value-font-weight` | | `--vaadin-input-field-value-line-height` | | `--vaadin-item-overlay-padding` | | `--vaadin-select-overlay-width` | ### Internal components In addition to `<vaadin-select>` itself, the following internal components are themable: - `<vaadin-select-value-button>` - has the same API as [`<vaadin-button>`](#/elements/vaadin-button). - `<vaadin-select-list-box>` - has the same API as [`<vaadin-list-box>`](#/elements/vaadin-list-box). - `<vaadin-select-item>` - has the same API as [`<vaadin-item>`](#/elements/vaadin-item). See [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation."""

    tag = "vaadin-select"
    schema = ComponentSchema(
        tag="vaadin-select",
        class_name="VaadinSelect",
        summary="`<vaadin-select>` is a Web Component for selecting values from a list of items.\n\n### Items\n\nUse the `items` property to define possible options for the select:\n\n```html\n<vaadin-select id=\"select\"></vaadin-select>\n```\n```js\nconst select = document.querySelector('#select');\nselect.items = [\n  { label: 'Most recent first', value: 'recent' },\n  { component: 'hr' },\n  { label: 'Rating: low to high', value: 'rating-asc', className: 'asc' },\n  { label: 'Rating: high to low', value: 'rating-desc', className: 'desc' },\n  { component: 'hr' },\n  { label: 'Price: low to high', value: 'price-asc', disabled: true },\n  { label: 'Price: high to low', value: 'price-desc', disabled: true }\n];\n```\n\n### Rendering\n\nAlternatively, the content of the select can be populated by using the renderer callback function.\n\nThe renderer function provides `root`, `select` arguments.\nGenerate DOM content, append it to the `root` element and control the state\nof the host element by accessing `select`.\n\n```js\nconst select = document.querySelector('#select');\nselect.renderer = function(root, select) {\n  const listBox = document.createElement('vaadin-list-box');\n  // append 3 <vaadin-item> elements\n  ['Jose', 'Manolo', 'Pedro'].forEach(function(name) {\n    const item = document.createElement('vaadin-item');\n    item.textContent = name;\n    item.setAttribute('label', name)\n    listBox.appendChild(item);\n  });\n\n  // update the content\n  root.appendChild(listBox);\n};\n```\n\nRenderer is called on initialization of new select and on its opening.\nDOM generated during the renderer call can be reused\nin the next renderer call and will be provided with the `root` argument.\nOn first call it will be empty.\n\n* Hint: By setting the `label` property of inner vaadin-items you will\nbe able to change the visual representation of the selected value in the input part.\n\n### Styling\n\nThe following shadow DOM parts are available for styling:\n\nPart name            | Description\n---------------------|----------------\n`label`              | The label element\n`input-field`        | The element that wraps prefix, value and toggle button\n`field-button`       | Set on the toggle button\n`error-message`      | The error message element\n`helper-text`        | The helper text element wrapper\n`required-indicator` | The `required` state indicator element\n`toggle-button`      | The toggle button\n`backdrop`           | Backdrop of the overlay\n`overlay`            | The overlay container\n`content`            | The overlay content\n\nThe following state attributes are available for styling:\n\nAttribute            | Description\n---------------------|---------------------------------\n`disabled`           | Set when the element is disabled\n`has-value`          | Set when the element has a value\n`has-label`          | Set when the element has a label\n`has-helper`         | Set when the element has helper text or slot\n`has-error-message`  | Set when the element has an error message\n`invalid`            | Set when the element is invalid\n`focused`            | Set when the element is focused\n`focus-ring`         | Set when the element is keyboard focused\n`readonly`           | Set when the element is readonly\n`opened`             | Set when the overlay is opened\n`phone`              | Set when the overlay is shown in phone mode\n\nThe following custom CSS properties are available for styling:\n\nCustom CSS property                                |\n:--------------------------------------------------|\n| `--vaadin-field-default-width`                   |\n| `--vaadin-input-field-background`                |\n| `--vaadin-input-field-border-color`              |\n| `--vaadin-input-field-border-radius`             |\n| `--vaadin-input-field-border-width`              |\n| `--vaadin-input-field-bottom-end-radius`         |\n| `--vaadin-input-field-bottom-start-radius`       |\n| `--vaadin-input-field-button-text-color`         |\n| `--vaadin-input-field-container-gap`             |\n| `--vaadin-input-field-disabled-background`       |\n| `--vaadin-input-field-disabled-text-color`       |\n| `--vaadin-input-field-error-color`               |\n| `--vaadin-input-field-error-font-size`           |\n| `--vaadin-input-field-error-font-weight`         |\n| `--vaadin-input-field-error-line-height`         |\n| `--vaadin-input-field-gap`                       |\n| `--vaadin-input-field-helper-color`              |\n| `--vaadin-input-field-helper-font-size`          |\n| `--vaadin-input-field-helper-font-weight`        |\n| `--vaadin-input-field-helper-line-height`        |\n| `--vaadin-input-field-label-color`               |\n| `--vaadin-input-field-label-font-size`           |\n| `--vaadin-input-field-label-font-weight`         |\n| `--vaadin-input-field-label-line-height`         |\n| `--vaadin-input-field-padding`                   |\n| `--vaadin-input-field-placeholder-color`         |\n| `--vaadin-input-field-required-indicator`        |\n| `--vaadin-input-field-required-indicator-color`  |\n| `--vaadin-input-field-top-end-radius`            |\n| `--vaadin-input-field-top-start-radius`          |\n| `--vaadin-input-field-value-color`               |\n| `--vaadin-input-field-value-font-size`           |\n| `--vaadin-input-field-value-font-weight`         |\n| `--vaadin-input-field-value-line-height`         |\n| `--vaadin-item-overlay-padding`                  |\n| `--vaadin-select-overlay-width`                  |\n\n### Internal components\n\nIn addition to `<vaadin-select>` itself, the following internal\ncomponents are themable:\n\n- `<vaadin-select-value-button>` - has the same API as [`<vaadin-button>`](#/elements/vaadin-button).\n- `<vaadin-select-list-box>` - has the same API as [`<vaadin-list-box>`](#/elements/vaadin-list-box).\n- `<vaadin-select-item>` - has the same API as [`<vaadin-item>`](#/elements/vaadin-item).\n\nSee [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation.",
        props=(
            PropertySchema(
                name="accessible-name",
                kind="string",
                choices=(),
                default=None,
                description="String used to label the component to screen reader users.",
            ),
            PropertySchema(
                name="accessible-name-ref",
                kind="string",
                choices=(),
                default=None,
                description="Id of the element used as label of the component to screen reader users.",
            ),
            PropertySchema(
                name="autofocus",
                kind="boolean",
                choices=(),
                default=None,
                description="Specify that this control should have input focus when the page loads.",
            ),
            PropertySchema(
                name="disabled", kind="boolean", choices=(), default=None, description="If true, the user cannot interact with this element."
            ),
            PropertySchema(name="error-message", kind="string", choices=(), default=None, description="Error to show when the field is invalid."),
            PropertySchema(name="helper-text", kind="string", choices=(), default=None, description="String used for the helper text."),
            PropertySchema(name="invalid", kind="boolean", choices=(), default=None, description="Set to true when the field is invalid."),
            PropertySchema(
                name="label",
                kind="string",
                choices=(),
                default=None,
                description="The label text for the input node.\nWhen no light dom defined via [slot=label], this value will be used.",
            ),
            PropertySchema(
                name="manual-validation",
                kind="boolean",
                choices=(),
                default=None,
                description="Set to true to enable manual validation mode. This mode disables automatic\nconstraint validation, allowing you to control the validation process yourself.\nYou can still trigger constraint validation manually with the `validate()` method\nor use `checkValidity()` to assess the component's validity without affecting\nthe invalid state. In manual validation mode, you can also manipulate\nthe `invalid` property directly through your application logic without conflicts\nwith the component's internal validation.",
            ),
            PropertySchema(name="name", kind="string", choices=(), default=None, description="The name of this element."),
            PropertySchema(
                name="no-vertical-overlap",
                kind="boolean",
                choices=(),
                default=None,
                description="Defines whether the overlay should overlap the target element\nin the y-axis, or be positioned right above/below it.",
            ),
            PropertySchema(name="opened", kind="boolean", choices=(), default=None, description="Set when the select is open"),
            PropertySchema(
                name="placeholder",
                kind="string",
                choices=(),
                default=None,
                description="A hint to the user of what can be entered in the control.\nThe placeholder will be displayed in the case that there\nis no item selected, or the selected item has an empty\nstring label, or the selected item has no label and it's\nDOM content is empty.",
            ),
            PropertySchema(
                name="readonly", kind="boolean", choices=(), default=None, description="When present, it specifies that the element is read-only."
            ),
            PropertySchema(
                name="renderer",
                kind="json",
                choices=(),
                type_text="SelectRenderer | undefined",
                default=None,
                description="Custom function for rendering the content of the `<vaadin-select>`.\nReceives two arguments:\n\n- `root` The internal container DOM element. Append your content to it.\n- `select` The reference to the `<vaadin-select>` element.",
            ),
            PropertySchema(name="required", kind="boolean", choices=(), default=None, description="Specifies that the user must fill in a value."),
            PropertySchema(
                name="value",
                kind="string",
                choices=(),
                default=None,
                description="The `value` property of the selected item, or an empty string\nif no item is selected.\nOn change or initialization, the component finds the item which matches the\nvalue and displays it.\nIf no value is provided to the component, it selects the first item without\nvalue or empty value.\nHint: If you do not want to select any item by default, you can either set all\nthe values of inner vaadin-items, or set the vaadin-select value to\nan inexistent value in the items list.",
            ),
            PropertySchema(
                name="theme",
                kind="string",
                choices=(),
                default=None,
                description="Space-separated theme variants, such as `primary` or `small`, which the element propagates to its parts.",
            ),
        ),
        fields=(
            PropertySchema(
                name="items",
                kind="json",
                choices=(),
                type_text="Array<SelectItem>",
                default=None,
                description="An array containing items that will be rendered as the options of the select.\n\n#### Example\n```js\nselect.items = [\n  { label: 'Most recent first', value: 'recent' },\n  { component: 'hr' },\n  { label: 'Rating: low to high', value: 'rating-asc', className: 'asc' },\n  { label: 'Rating: high to low', value: 'rating-desc', className: 'desc' },\n  { component: 'hr' },\n  { label: 'Price: low to high', value: 'price-asc', disabled: true },\n  { label: 'Price: high to low', value: 'price-desc', disabled: true }\n];\n```\n\nNote: each item is rendered by default as the internal `<vaadin-select-item>` that is an extension of `<vaadin-item>`.\nTo render the item with a custom component, provide a tag name by the `component` property.",
            ),
        ),
        events=("change", "invalid-changed", "opened-changed", "validated", "value-changed"),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        accessible_name: str | None = None,
        accessible_name_ref: str | None = None,
        autofocus: bool | None = None,
        disabled: bool | None = None,
        error_message: str | None = None,
        helper_text: str | None = None,
        invalid: bool | None = None,
        label: str | None = None,
        manual_validation: bool | None = None,
        name: str | None = None,
        no_vertical_overlap: bool | None = None,
        opened: bool | None = None,
        placeholder: str | None = None,
        readonly: bool | None = None,
        renderer: Any = None,
        required: bool | None = None,
        value: str | None = None,
        theme: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "accessible-name": accessible_name,
                "accessible-name-ref": accessible_name_ref,
                "autofocus": autofocus,
                "disabled": disabled,
                "error-message": error_message,
                "helper-text": helper_text,
                "invalid": invalid,
                "label": label,
                "manual-validation": manual_validation,
                "name": name,
                "no-vertical-overlap": no_vertical_overlap,
                "opened": opened,
                "placeholder": placeholder,
                "readonly": readonly,
                "renderer": renderer,
                "required": required,
                "value": value,
                "theme": theme,
            },
            **props,
        )


class VaadinTab(Component):
    """`<vaadin-tab>` is a Web Component providing an accessible and customizable tab. ```html <vaadin-tab>Tab 1</vaadin-tab> ``` The following state attributes are available for styling: Attribute | Description ---------------|--------------------------------- `disabled` | Set when the element is disabled `focused` | Set when the element is focused `focus-ring` | Set when the element is keyboard focused `selected` | Set when the tab is selected `active` | Set when mousedown or enter/spacebar pressed `orientation` | Set to `horizontal` or `vertical` depending on the direction of items `has-tooltip` | Set when the tab has a slotted tooltip The following custom CSS properties are available for styling: Custom CSS property | :------------------------------| | `--vaadin-tab-background` | | `--vaadin-tab-border-color` | | `--vaadin-tab-border-radius` | | `--vaadin-tab-border-width` | | `--vaadin-tab-font-size` | | `--vaadin-tab-font-weight` | | `--vaadin-tab-gap` | | `--vaadin-tab-line-height` | | `--vaadin-tab-padding` | | `--vaadin-tab-text-color` | See [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation."""

    tag = "vaadin-tab"
    schema = ComponentSchema(
        tag="vaadin-tab",
        class_name="VaadinTab",
        summary="`<vaadin-tab>` is a Web Component providing an accessible and customizable tab.\n\n```html\n<vaadin-tab>Tab 1</vaadin-tab>\n```\n\nThe following state attributes are available for styling:\n\nAttribute      | Description\n---------------|---------------------------------\n`disabled`     | Set when the element is disabled\n`focused`      | Set when the element is focused\n`focus-ring`   | Set when the element is keyboard focused\n`selected`     | Set when the tab is selected\n`active`       | Set when mousedown or enter/spacebar pressed\n`orientation`  | Set to `horizontal` or `vertical` depending on the direction of items\n`has-tooltip`  | Set when the tab has a slotted tooltip\n\nThe following custom CSS properties are available for styling:\n\nCustom CSS property            |\n:------------------------------|\n| `--vaadin-tab-background`    |\n| `--vaadin-tab-border-color`  |\n| `--vaadin-tab-border-radius` |\n| `--vaadin-tab-border-width`  |\n| `--vaadin-tab-font-size`     |\n| `--vaadin-tab-font-weight`   |\n| `--vaadin-tab-gap`           |\n| `--vaadin-tab-line-height`   |\n| `--vaadin-tab-padding`       |\n| `--vaadin-tab-text-color`    |\n\nSee [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation.",
        props=(
            PropertySchema(
                name="disabled", kind="boolean", choices=(), default=None, description="If true, the user cannot interact with this element."
            ),
            PropertySchema(name="selected", kind="boolean", choices=(), default=None, description="If true, the item is in selected state."),
            PropertySchema(
                name="theme",
                kind="string",
                choices=(),
                default=None,
                description="Space-separated theme variants, such as `primary` or `small`, which the element propagates to its parts.",
            ),
        ),
        events=(),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        disabled: bool | None = None,
        selected: bool | None = None,
        theme: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "disabled": disabled,
                "selected": selected,
                "theme": theme,
            },
            **props,
        )


class VaadinTabs(Component):
    """`<vaadin-tabs>` is a Web Component for organizing and grouping content into sections. ```html <vaadin-tabs selected="4"> <vaadin-tab>Page 1</vaadin-tab> <vaadin-tab>Page 2</vaadin-tab> <vaadin-tab>Page 3</vaadin-tab> <vaadin-tab>Page 4</vaadin-tab> </vaadin-tabs> ``` ### Styling The following shadow DOM parts are available for styling: Part name | Description ------------------|-------------------------------------- `back-button` | Button for moving the scroll back `tabs` | The tabs container `forward-button` | Button for moving the scroll forward The following state attributes are available for styling: Attribute | Description ---------------|-------------------------------------- `orientation` | Tabs disposition, valid values are `horizontal` and `vertical` `overflow` | It's set to `start`, `end`, none or both. The following custom CSS properties are available for styling: Custom CSS property | :--------------------------------| | `--vaadin-tabs-background` | | `--vaadin-tabs-border-color` | | `--vaadin-tabs-border-radius` | | `--vaadin-tabs-border-width` | | `--vaadin-tabs-font-size` | | `--vaadin-tabs-font-weight` | | `--vaadin-tabs-gap` | | `--vaadin-tabs-padding` | See [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation."""

    tag = "vaadin-tabs"
    schema = ComponentSchema(
        tag="vaadin-tabs",
        class_name="VaadinTabs",
        summary='`<vaadin-tabs>` is a Web Component for organizing and grouping content into sections.\n\n```html\n<vaadin-tabs selected="4">\n  <vaadin-tab>Page 1</vaadin-tab>\n  <vaadin-tab>Page 2</vaadin-tab>\n  <vaadin-tab>Page 3</vaadin-tab>\n  <vaadin-tab>Page 4</vaadin-tab>\n</vaadin-tabs>\n```\n\n### Styling\n\nThe following shadow DOM parts are available for styling:\n\nPart name         | Description\n------------------|--------------------------------------\n`back-button`     | Button for moving the scroll back\n`tabs`            | The tabs container\n`forward-button`  | Button for moving the scroll forward\n\nThe following state attributes are available for styling:\n\nAttribute      | Description\n---------------|--------------------------------------\n`orientation`  | Tabs disposition, valid values are `horizontal` and `vertical`\n`overflow`     | It\'s set to `start`, `end`, none or both.\n\nThe following custom CSS properties are available for styling:\n\nCustom CSS property              |\n:--------------------------------|\n| `--vaadin-tabs-background`     |\n| `--vaadin-tabs-border-color`   |\n| `--vaadin-tabs-border-radius`  |\n| `--vaadin-tabs-border-width`   |\n| `--vaadin-tabs-font-size`      |\n| `--vaadin-tabs-font-weight`    |\n| `--vaadin-tabs-gap`            |\n| `--vaadin-tabs-padding`        |\n\nSee [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation.',
        props=(
            PropertySchema(
                name="disabled",
                kind="boolean",
                choices=(),
                default=None,
                description="If true, the user cannot interact with this element.\nWhen the element is disabled, the selected item is\nnot updated when `selected` property is changed.",
            ),
            PropertySchema(
                name="orientation",
                kind="string",
                choices=(),
                default=None,
                description="Set tabs disposition. Possible values are `horizontal|vertical`",
            ),
            PropertySchema(name="selected", kind="number", choices=(), default=None, description="The index of the selected tab."),
            PropertySchema(
                name="theme",
                kind="string",
                choices=(),
                default=None,
                description="Space-separated theme variants, such as `primary` or `small`, which the element propagates to its parts.",
            ),
        ),
        events=("items-changed", "selected-changed"),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        disabled: bool | None = None,
        orientation: str | None = None,
        selected: float | None = None,
        theme: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "disabled": disabled,
                "orientation": orientation,
                "selected": selected,
                "theme": theme,
            },
            **props,
        )


class VaadinTextField(Component):
    """`<vaadin-text-field>` is a web component that allows the user to input and edit text. ```html <vaadin-text-field label="First Name"></vaadin-text-field> ``` ### Prefixes and suffixes These are child elements of a `<vaadin-text-field>` that are displayed inline with the input, before or after. In order for an element to be considered as a prefix, it must have the slot attribute set to `prefix` (and similarly for `suffix`). ```html <vaadin-text-field label="Email address"> <div slot="prefix">Sent to:</div> <div slot="suffix">@vaadin.com</div> </vaadin-text-field> ``` ### Styling The following shadow DOM parts are available for styling: Part name | Description ---------------------|---------------- `label` | The label element `input-field` | The element that wraps prefix, value and suffix `field-button` | Set on the clear button `clear-button` | The clear button `error-message` | The error message element `helper-text` | The helper text element wrapper `required-indicator` | The `required` state indicator element The following state attributes are available for styling: Attribute | Description ---------------------|--------------------------------- `disabled` | Set when the element is disabled `has-value` | Set when the element has a value `has-label` | Set when the element has a label `has-helper` | Set when the element has helper text or slot `has-error-message` | Set when the element has an error message `has-tooltip` | Set when the element has a slotted tooltip `invalid` | Set when the element is invalid `input-prevented` | Temporarily set when invalid input is prevented `focused` | Set when the element is focused `focus-ring` | Set when the element is keyboard focused `readonly` | Set when the element is readonly Note, the `input-prevented` state attribute is only supported when `allowedCharPattern` is set. The following custom CSS properties are available for styling: Custom CSS property | :--------------------------------------------------| | `--vaadin-field-default-width` | | `--vaadin-input-field-background` | | `--vaadin-input-field-border-color` | | `--vaadin-input-field-border-radius` | | `--vaadin-input-field-border-width` | | `--vaadin-input-field-bottom-end-radius` | | `--vaadin-input-field-bottom-start-radius` | | `--vaadin-input-field-button-text-color` | | `--vaadin-input-field-container-gap` | | `--vaadin-input-field-disabled-background` | | `--vaadin-input-field-disabled-text-color` | | `--vaadin-input-field-error-color` | | `--vaadin-input-field-error-font-size` | | `--vaadin-input-field-error-font-weight` | | `--vaadin-input-field-error-line-height` | | `--vaadin-input-field-gap` | | `--vaadin-input-field-helper-color` | | `--vaadin-input-field-helper-font-size` | | `--vaadin-input-field-helper-font-weight` | | `--vaadin-input-field-helper-line-height` | | `--vaadin-input-field-label-color` | | `--vaadin-input-field-label-font-size` | | `--vaadin-input-field-label-font-weight` | | `--vaadin-input-field-label-line-height` | | `--vaadin-input-field-padding` | | `--vaadin-input-field-placeholder-color` | | `--vaadin-input-field-required-indicator` | | `--vaadin-input-field-required-indicator-color` | | `--vaadin-input-field-top-end-radius` | | `--vaadin-input-field-top-start-radius` | | `--vaadin-input-field-value-color` | | `--vaadin-input-field-value-font-size` | | `--vaadin-input-field-value-font-weight` | | `--vaadin-input-field-value-line-height` | See [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation."""

    tag = "vaadin-text-field"
    schema = ComponentSchema(
        tag="vaadin-text-field",
        class_name="VaadinTextField",
        summary='`<vaadin-text-field>` is a web component that allows the user to input and edit text.\n\n```html\n<vaadin-text-field label="First Name"></vaadin-text-field>\n```\n\n### Prefixes and suffixes\n\nThese are child elements of a `<vaadin-text-field>` that are displayed\ninline with the input, before or after.\nIn order for an element to be considered as a prefix, it must have the slot\nattribute set to `prefix` (and similarly for `suffix`).\n\n```html\n<vaadin-text-field label="Email address">\n  <div slot="prefix">Sent to:</div>\n  <div slot="suffix">@vaadin.com</div>\n</vaadin-text-field>\n```\n\n### Styling\n\nThe following shadow DOM parts are available for styling:\n\nPart name            | Description\n---------------------|----------------\n`label`              | The label element\n`input-field`        | The element that wraps prefix, value and suffix\n`field-button`       | Set on the clear button\n`clear-button`       | The clear button\n`error-message`      | The error message element\n`helper-text`        | The helper text element wrapper\n`required-indicator` | The `required` state indicator element\n\nThe following state attributes are available for styling:\n\nAttribute            | Description\n---------------------|---------------------------------\n`disabled`           | Set when the element is disabled\n`has-value`          | Set when the element has a value\n`has-label`          | Set when the element has a label\n`has-helper`         | Set when the element has helper text or slot\n`has-error-message`  | Set when the element has an error message\n`has-tooltip`        | Set when the element has a slotted tooltip\n`invalid`            | Set when the element is invalid\n`input-prevented`    | Temporarily set when invalid input is prevented\n`focused`            | Set when the element is focused\n`focus-ring`         | Set when the element is keyboard focused\n`readonly`           | Set when the element is readonly\n\nNote, the `input-prevented` state attribute is only supported when `allowedCharPattern` is set.\n\nThe following custom CSS properties are available for styling:\n\nCustom CSS property                                |\n:--------------------------------------------------|\n| `--vaadin-field-default-width`                   |\n| `--vaadin-input-field-background`                |\n| `--vaadin-input-field-border-color`              |\n| `--vaadin-input-field-border-radius`             |\n| `--vaadin-input-field-border-width`              |\n| `--vaadin-input-field-bottom-end-radius`         |\n| `--vaadin-input-field-bottom-start-radius`       |\n| `--vaadin-input-field-button-text-color`         |\n| `--vaadin-input-field-container-gap`             |\n| `--vaadin-input-field-disabled-background`       |\n| `--vaadin-input-field-disabled-text-color`       |\n| `--vaadin-input-field-error-color`               |\n| `--vaadin-input-field-error-font-size`           |\n| `--vaadin-input-field-error-font-weight`         |\n| `--vaadin-input-field-error-line-height`         |\n| `--vaadin-input-field-gap`                       |\n| `--vaadin-input-field-helper-color`              |\n| `--vaadin-input-field-helper-font-size`          |\n| `--vaadin-input-field-helper-font-weight`        |\n| `--vaadin-input-field-helper-line-height`        |\n| `--vaadin-input-field-label-color`               |\n| `--vaadin-input-field-label-font-size`           |\n| `--vaadin-input-field-label-font-weight`         |\n| `--vaadin-input-field-label-line-height`         |\n| `--vaadin-input-field-padding`                   |\n| `--vaadin-input-field-placeholder-color`         |\n| `--vaadin-input-field-required-indicator`        |\n| `--vaadin-input-field-required-indicator-color`  |\n| `--vaadin-input-field-top-end-radius`            |\n| `--vaadin-input-field-top-start-radius`          |\n| `--vaadin-input-field-value-color`               |\n| `--vaadin-input-field-value-font-size`           |\n| `--vaadin-input-field-value-font-weight`         |\n| `--vaadin-input-field-value-line-height`         |\n\nSee [Styling Components](https://vaadin.com/docs/latest/styling/styling-components) documentation.',
        props=(
            PropertySchema(
                name="accessible-name",
                kind="string",
                choices=(),
                default=None,
                description="String used to label the component to screen reader users.",
            ),
            PropertySchema(
                name="accessible-name-ref",
                kind="string",
                choices=(),
                default=None,
                description="Id of the element used as label of the component to screen reader users.",
            ),
            PropertySchema(
                name="allowed-char-pattern",
                kind="string",
                choices=(),
                default=None,
                description="A pattern matched against individual characters the user inputs.\n\nWhen set, the field will prevent:\n- `keydown` events if the entered key doesn't match `/^allowedCharPattern$/`\n- `paste` events if the pasted text doesn't match `/^allowedCharPattern*$/`\n- `drop` events if the dropped text doesn't match `/^allowedCharPattern*$/`\n\nFor example, to allow entering only numbers and minus signs, use:\n`allowedCharPattern = \"[\\\\d-]\"`",
            ),
            PropertySchema(
                name="autocapitalize",
                kind="string",
                choices=(),
                default=None,
                description="This is a property supported by Safari and Chrome that is used to control whether\nautocapitalization should be enabled when the user is entering/editing the text.\nPossible values are:\ncharacters: Characters capitalization.\nwords: Words capitalization.\nsentences: Sentences capitalization.\nnone: No capitalization.",
            ),
            PropertySchema(
                name="autocomplete",
                kind="string",
                choices=(),
                default=None,
                description="Whether the value of the control can be automatically completed by the browser.\nList of available options at:\nhttps://developer.mozilla.org/en/docs/Web/HTML/Element/input#attr-autocomplete",
            ),
            PropertySchema(
                name="autocorrect",
                kind="string",
                choices=(),
                default=None,
                description="This is a property supported by Safari that is used to control whether\nautocorrection should be enabled when the user is entering/editing the text.\nPossible values are:\non: Enable autocorrection.\noff: Disable autocorrection.",
            ),
            PropertySchema(
                name="autofocus",
                kind="boolean",
                choices=(),
                default=None,
                description="Specify that this control should have input focus when the page loads.",
            ),
            PropertySchema(
                name="autoselect",
                kind="boolean",
                choices=(),
                default=None,
                description="If true, the input text gets fully selected when the field is focused using click or touch / tap.",
            ),
            PropertySchema(
                name="clear-button-visible",
                kind="boolean",
                choices=(),
                default=None,
                description="Set to true to display the clear icon which clears the input.\nThis also enables clearing the input when pressing Esc key.",
            ),
            PropertySchema(
                name="disabled", kind="boolean", choices=(), default=None, description="If true, the user cannot interact with this element."
            ),
            PropertySchema(name="error-message", kind="string", choices=(), default=None, description="Error to show when the field is invalid."),
            PropertySchema(name="helper-text", kind="string", choices=(), default=None, description="String used for the helper text."),
            PropertySchema(name="invalid", kind="boolean", choices=(), default=None, description="Set to true when the field is invalid."),
            PropertySchema(
                name="label",
                kind="string",
                choices=(),
                default=None,
                description="The label text for the input node.\nWhen no light dom defined via [slot=label], this value will be used.",
            ),
            PropertySchema(
                name="manual-validation",
                kind="boolean",
                choices=(),
                default=None,
                description="Set to true to enable manual validation mode. This mode disables automatic\nconstraint validation, allowing you to control the validation process yourself.\nYou can still trigger constraint validation manually with the `validate()` method\nor use `checkValidity()` to assess the component's validity without affecting\nthe invalid state. In manual validation mode, you can also manipulate\nthe `invalid` property directly through your application logic without conflicts\nwith the component's internal validation.",
            ),
            PropertySchema(
                name="maxlength",
                kind="number",
                choices=(),
                default=None,
                description="Maximum number of characters (in Unicode code points) that the user can enter.",
            ),
            PropertySchema(
                name="minlength",
                kind="number",
                choices=(),
                default=None,
                description="Minimum number of characters (in Unicode code points) that the user can enter.",
            ),
            PropertySchema(name="name", kind="string", choices=(), default=None, description="The name of this field."),
            PropertySchema(
                name="pattern",
                kind="string",
                choices=(),
                default=None,
                description="A regular expression that the value is checked against.\nThe pattern must match the entire value, not just some subset.",
            ),
            PropertySchema(
                name="placeholder", kind="string", choices=(), default=None, description="A hint to the user of what can be entered in the field."
            ),
            PropertySchema(
                name="readonly", kind="boolean", choices=(), default=None, description="When present, it specifies that the field is read-only."
            ),
            PropertySchema(name="required", kind="boolean", choices=(), default=None, description="Specifies that the user must fill in a value."),
            PropertySchema(
                name="title",
                kind="string",
                choices=(),
                default=None,
                description="The text usually displayed in a tooltip popup when the mouse is over the field.",
            ),
            PropertySchema(name="value", kind="string", choices=(), default=None, description="The value of the field."),
            PropertySchema(
                name="theme",
                kind="string",
                choices=(),
                default=None,
                description="Space-separated theme variants, such as `primary` or `small`, which the element propagates to its parts.",
            ),
        ),
        events=("change", "input", "invalid-changed", "validated", "value-changed"),
        slots=(),
    )

    def __init__(
        self,
        *children: Child,
        key: str | None = None,
        accessible_name: str | None = None,
        accessible_name_ref: str | None = None,
        allowed_char_pattern: str | None = None,
        autocapitalize: str | None = None,
        autocomplete: str | None = None,
        autocorrect: str | None = None,
        autofocus: bool | None = None,
        autoselect: bool | None = None,
        clear_button_visible: bool | None = None,
        disabled: bool | None = None,
        error_message: str | None = None,
        helper_text: str | None = None,
        invalid: bool | None = None,
        label: str | None = None,
        manual_validation: bool | None = None,
        maxlength: float | None = None,
        minlength: float | None = None,
        name: str | None = None,
        pattern: str | None = None,
        placeholder: str | None = None,
        readonly: bool | None = None,
        required: bool | None = None,
        title: str | None = None,
        value: str | None = None,
        theme: str | None = None,
        **props: Any,
    ) -> None:
        super().__init__(
            *children,
            key=key,
            props={
                "accessible-name": accessible_name,
                "accessible-name-ref": accessible_name_ref,
                "allowed-char-pattern": allowed_char_pattern,
                "autocapitalize": autocapitalize,
                "autocomplete": autocomplete,
                "autocorrect": autocorrect,
                "autofocus": autofocus,
                "autoselect": autoselect,
                "clear-button-visible": clear_button_visible,
                "disabled": disabled,
                "error-message": error_message,
                "helper-text": helper_text,
                "invalid": invalid,
                "label": label,
                "manual-validation": manual_validation,
                "maxlength": maxlength,
                "minlength": minlength,
                "name": name,
                "pattern": pattern,
                "placeholder": placeholder,
                "readonly": readonly,
                "required": required,
                "title": title,
                "value": value,
                "theme": theme,
            },
            **props,
        )
